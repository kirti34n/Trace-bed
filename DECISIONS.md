# DECISIONS.md — append-only

Format: **Decision · Context · Alternatives considered · Rationale · Date.** Log deviations, dependencies (with licenses), and spec corrections — not micro-choices. Never edit an entry; supersede with a new one.

---

## D-001 Rename: Strata → Tracebed
**Decision:** The service, package, schema, and CLI are named `tracebed`. **Context:** "Strata" collides with Palo Alto Networks' Strata security product family (sold to this exact SOC/BFSI buyer) and Strata Identity (strata.io), which ships identity orchestration for AI agents; both `strata` package names are squatted (PyPI, npm). **Alternatives:** Moraine, Patina (runners-up; PyPI collisions or weaker fit). **Rationale:** Tracebed names the architectural thesis ("layered memory over a trace bed"; nothing is remembered that doesn't stand on a trace) and has a clean sweep: PyPI, npm, GitHub org all free. **Date 2026-07-25.**

## D-002 Postgres 18, not 16/17
**Decision:** Target PostgreSQL 18. **Context:** Spec §12 said 16/17; PG18 went GA 2025-09-25, ten months before the spec's own datestamp. **Alternatives:** PG16 (one line from the back of the support window), PG17. **Rationale:** pg_textsearch requires PG17+; PG18's async I/O directly benefits a disk-resident HNSW index — the exact hot-path failure mode; greenfield services should not start near EOL. **Date 2026-07-25.**

## D-003 Lexical default: pg_textsearch (true BM25, PostgreSQL License); ts_rank demoted
**Decision:** BM25 via TigerData's `pg_textsearch` is the lexical default; native `ts_rank` becomes a zero-extension fallback only. **Context:** BEIR SciFact nDCG@10: ts_rank **0.07**, BM25 0.69, pgvector 0.66, RRF hybrid 0.70 — fusion does not rescue ts_rank, it grants a 0.07 ranker equal reciprocal-rank weight. Decisive second reason: §7.5's rarity gate ("≥2 rare terms") **is** an IDF computation and ts_rank has no IDF — the abstention gate's signal source would not exist. **Alternatives:** ParadeDB pg_search (AGPL-3.0, stays listed as opt-in), ts_rank default (refuted). **Rationale:** True BM25 under the PostgreSQL License removes AGPL from the lexical slot entirely — a strict win for the license invariant. Caveat recorded: pg_textsearch is young (v1.x, 2026). **Date 2026-07-25.**

## D-004 Python 3.13 minimum
**Decision:** Python 3.13 (GIL build; do not chase free-threading). **Context:** Prompt rule 10 pinned 3.12, which has been security-fix-only since ~April 2025. **Alternatives:** 3.12 (stale), 3.14 target later. **Rationale:** Greenfield service on a maintained branch; the dependency set (FastAPI, Pydantic v2, psycopg 3) all support 3.13. **Date 2026-07-25.**

## D-005 License policy: allowlist + denylist; LGPL-3.0 permitted for dynamically-linked unmodified libraries
**Decision:** Allow PostgreSQL / Apache-2.0 / BSD / MIT / ISC, **plus LGPL-3.0-only for unmodified, dynamically-linked libraries with recorded rationale**. Hard denylist: SSPL, BSL, RSAL, Elastic-v2. AGPL only behind driver interfaces as opt-in, never default. **Context:** psycopg 3 — mandated by the same prompt whose rule 9 allowlist excludes it — is LGPL-3.0-only; the CI license gate would fail on commit one. **Alternatives:** swap to asyncpg (Apache-2.0); ban LGPL. **Rationale:** LGPL is not viral across process/link boundaries for an unmodified library; psycopg 3 is the better-supported driver for this design. Recorded LGPL dependency: **psycopg 3 (LGPL-3.0-only, used unmodified, dynamically linked)**. **Date 2026-07-25.**

## D-006 MinIO demoted to legacy compatibility only
**Decision:** Trace-archive S3 targets are SeaweedFS (Apache-2.0) and the filesystem driver; MinIO is "legacy compatibility with an existing deployment only — not for new installs." **Context:** MinIO's OSS repo was archived 2026-04-25; unmaintained, source-only, admin console previously stripped. **Alternatives:** keep MinIO as listed opt-in (misleading). **Rationale:** The S3 driver speaks generic S3 (no MinIO SDK), so legacy MinIO still works without endorsing it. Side-finding forwarded to the Atom owner: Atom's compliance bucket pins the now-frozen `minio/minio:latest`. **Date 2026-07-25.**

## D-007 Embedding pin: Gemini `gemini-embedding-2`, halfvec, ≤768 dims, stamped per row
**Decision:** Default embeddings (indexing **and** query) are Gemini `gemini-embedding-2` behind `EmbeddingPort`; model id + version + dimension stamped on every `memory_item` row; `halfvec(768)`. A pinned local ONNX driver is a fully supported secondary for air-gapped/latency-sensitive deployments. Re-embedding is an explicit versioned migration (new `embedding_model` row + backfill job); mixed-model retrieval is refused. **Context:** Nothing in the spec pinned the embedding model; a silent swap invalidates the entire vault — the same argument the spec itself makes for pinning judges. **Alternatives:** local-only ONNX default (worse accuracy; kept as secondary), caller-supplied vectors (pushes model deps into every runtime). **Rationale:** Human decision: accuracy beats latency. **Date 2026-07-25.**

## D-008 Worker generation: Gemini 3.1 Pro default via OpenAI-compatible interface; §13 privacy claim rewritten; scoring epochs
**Decision:** Contribution judge, shadow validator, and distiller default to **Gemini 3.1 Pro** (Flash configurable per worker) through an OpenAI-compatible `LLMProviderPort` (Google direct / LiteLLM / gateway = one config line). §13's "run content never leaves the deployment" is rewritten: query and trace content leaving the deployment is **accepted** in the default profile; the local ONNX embedding driver plus a local OpenAI-compatible endpoint is the documented answer for customers who refuse. A `scoring_epoch` (judge model id/version, sampling params, prompt hash) is stamped on every scored artifact; cross-epoch comparison is rejected. **Context:** The judge and validator gate Q and promotion — errors there corrupt the vault permanently; the spec's "local vLLM" mandate had no hardware floor and no epoch concept. **Alternatives:** pinned local open-weight models as default (kept as a profile); "vLLM-compatible" wording (needlessly narrow). **Rationale:** Human decision: accuracy beats latency/locality for the vault-gating workers. **Date 2026-07-25.**

## D-009 rrf_k = 60 in typed config, with per-arm weights
**Decision:** `retrieval.rrf_k = 60`, `rrf_weight_vector = rrf_weight_lexical = 1.0`, all typed config. **Context:** §14 omitted rrf_k entirely — a magic number by construction under rule 12. **Rationale:** Standard RRF constant; per-arm weights exist because unweighted RRF over unequal-quality arms measurably underperforms. **Date 2026-07-25.**

## D-010 Latency budget relaxed: 300ms p99 total, 200ms query-embed timeout, explicit degradation ladder
**Decision:** Total retrieval budget 300ms p99; query-embed sub-budget 200ms. Embed timeout → lexical-only; total budget exceeded → static prefix only; store error → nothing. Fail-open throughout. **Context:** The original 100ms p99 was unmeetable with a remote query embedding, and the audit showed the single-project warm bench certified a condition production never has. **Alternatives:** 100ms with local-only embedding (worse accuracy); no budget (unbounded). **Rationale:** Human decision: accuracy beats latency; the ladder keeps every failure mode fail-open and *observable* (`retrieval_event.outcome_code`). The bench becomes 50 projects × 100k, concurrent, and is informational (not CI-gating) until the human flips it. **Date 2026-07-25.**

## D-011 Q-update formula corrected: weight scales the learning rate, not the reward
**Decision:** `Q ← clamp01(Q + α·w·c·(r − Q))` with `r` = outcome polarity ∈ [0,1] from the adapter, `w` = adapter trust weight, `w = 0` short-circuits. Tie-break for the one-update-per-day cap: highest-w adapter, then earliest arrival; idempotent under replay via `outcome_event.event_id`. **Context:** Spec §9 fed the adapter weight in as the reward `r`: with Q starting at 0.5, a *successful* downstream event (weight 0.3) yields `r − Q = −0.2` and **lowers** the score — the update punishes success; implicit signals (0.0) would drive Q to zero, the exact guessed-reward failure invariant 4 forbids. **Alternatives:** none viable; the spec formula is arithmetically wrong. **Rationale:** The weight expresses how much the signal is trusted, not how good the outcome was. **Date 2026-07-25.**

## D-012 Phase 2 vault gate restated as a trend assertion; quarantine/candidate TTLs added
**Decision:** The Phase 2 gate asserts *net vault growth rate strictly decreasing across the soak, with a projected plateau date* — not an observed plateau. Quarantine TTL 30d, unpromoted-candidate TTL 45d (→ archived). **Context:** Decaying 5%/idle-week from 0.5 to the 0.15 floor takes ln(0.3)/ln(0.95) ≈ 23.5 weeks ≈ **164 days** against a 30-day soak — arithmetically unpassable; separately, quarantined and candidate rows had no exit, so no state had a bounded population. **Alternatives:** lengthen soak to ~180 days (slow), raise decay to ~15%/wk (changes semantics). **Rationale:** The trend assertion is measurable in 30 days and is what the metric actually needs to show. **Date 2026-07-25.**

## D-013 One state machine (spec §8 diagram and §12 enum reconciled)
**Decision:** Single status enum: `quarantined, candidate, validated, superseded, stale, retired, archived, pinned, tombstoned`; Trace/Extracted are not memory states; Tier A/B live in immutable `trust_tier`; the full legal/illegal transition table in PLAN.md §5 is the source of truth for table-driven tests. `archived → validated` (operator restore) exists; retirement from `validated` requires the principal threshold (D-021). **Context:** The spec's diagram and enum were two different machines; the mandated transition tests could not be written. **Date 2026-07-25.**

## D-014 Preferences and derived state: `pinned` status / separate table (SM-05)
**Decision:** Preferences live in `memory_item` with explicit ungoverned status `pinned` (operator provenance; only edges creation→pinned, pinned→tombstoned). Derived state moves to its own `derived_state` table outside the state machine (versioned deterministic overwrite is a different shape). **Context:** Both types had no legal initial status; "deterministic overwrite" is exactly what the machine forbids for governed rows. **Alternatives:** ungoverned flag on governed statuses (muddies the transition table). **Date 2026-07-25.**

## D-015 RRF orders; abstention thresholds calibrated raw signals
**Decision:** RRF (k=60) is used only to order the fused candidate list. The abstention decision thresholds **calibrated raw signals**: cosine similarity and saturation-normalized BM25 (`s/(s+k)`), plus the rarity gate computed from pg_textsearch per-project IDF (cold start: abstain below 200 corpus docs). The composite ranking score uses the calibrated signals, never RRF output. **Context:** Spec §7.3 fused with RRF then §7.4–7.5 thresholded a composite with a 0.40 similarity term — but RRF discards score magnitudes by construction; its output encodes consensus rank, not relevance, and a rank cannot be thresholded for "good enough to inject." **Date 2026-07-25.**

## D-016 Dynamic memory block placed last, after all cacheable content
**Decision:** `context_block.placement = "append_last"`. The static prefix (definition, pinned preferences, validated agent-type lessons) stays in the cacheable region; the dynamic block always renders after all cacheable content. **Context:** Provider prompt caching invalidates a changed block *and everything after it*; a dynamic block between the cached prefix and the conversation invalidates everything downstream on every run — destroying the prompt-cache economics the prefix builder exists to create. **Date 2026-07-25.**

## D-017 Isolation stack: server-derived project_id + typed repository + forced RLS + LIST partitions with a ceiling
**Decision:** (1) `project_id` derived server-side from the authenticated principal via `agent_registration` — never caller-asserted; (2) typed repository where every builder requires a `ProjectId` newtype, no scope-less constructor exists, and an AST lint bans raw SQL outside `stores/pg/`; (3) RLS with `FORCE ROW LEVEL SECURITY` on every partitioned table as backstop; (4) LIST partitioning as a lifecycle mechanism (O(1) project deletion) with a documented ceiling of **1,000 projects per instance** and a stated migration path to HASH partitioning behind the same repo API. Rule 6 restated: the wall covers **every key in every store** (Postgres, Valkey, trace store, exports), not just queries/sweeps/indexes. **Context:** The host (Atom) has no project entity and defaults to an unverified actor header; caller-asserted scope makes construction-layer partitioning security theatre; production leak reports concentrate on by-id routes, not search. **Date 2026-07-25.**

## D-018 The service mints run_id and returns it from retrieve()
**Decision:** `run_id` is a UUIDv7 minted server-side at `retrieve()` and returned; the SDK echoes it on `trace`/`feedback`/`propose_memory`. Runs that never call retrieve: SDK mints locally, flagged `origin=sdk`. **Context:** The host has no per-invocation identifier for standalone agents; without run_id every join in credit assignment breaks. This makes credit assignment work with zero host support. **Date 2026-07-25.**

## D-019 Tier A notes are template + enum, zero byte passthrough
**Decision:** Tier A operational notes are rendered exclusively from closed-vocabulary templates (error-class enums, tool ids, hashes, numeric fields); no byte of tool output, stderr, or validator text enters a note; raw text stays in the trace behind a pointer. Gate test: no substring ≥8 bytes from a tool error body appears in any note. **Context:** "No free text flows into the note" was false: tool error bodies echo attacker input, tracebacks quote payloads, and Pydantic v2 embeds the offending value as `input_value=` in validation errors — the "structured" note carried attacker text verbatim into same-run injectable candidates. **Alternatives:** keep free text with Tier B quarantine for Tier A (loses the immediacy that justifies Tier A). **Date 2026-07-25.**

## D-020 Corroboration requires computable independence; skip ships disabled
**Decision:** `trace_index` gains `submitter_principal` and `input_signature_hash` in Phase 0. Independence = **differing authenticated principal AND differing input-signature cluster** (64-bit SimHash, Hamming ≤ 8 = same cluster). The "2+ independent traces" shadow-validation skip ships **disabled** until both columns exist and Phase 3 enables it with this definition. `propose_memory` proposals are a provenance class that can never satisfy any skip (D-023). **Context:** From the spec's schema, "independent" was uncomputable (only run_id existed), so the skip degraded to "submitted twice" — a two-call Sybil bypass. GovMem (arXiv:2607.02579) measured naive count-based corroboration at a **0.597 false-promotion rate**; Tracebed's traces are maximally correlated by construction. **Date 2026-07-25.**

## D-021 outcome_event carries an authenticated principal; retirement needs K distinct principals
**Decision:** Every outcome_event records the authenticated `principal_id`. Automatic retirement (Q < 0.25 after ≥4 scored uses) additionally requires outcomes from ≥ **K = 3** distinct principals; below K, the memory routes to the review queue instead. **Context:** The Q arithmetic was a memory-destruction primitive: with c≈1, r=0 from 0.5: 0.35 → 0.245 → 0.172 → 0.120 — four scored uses, exactly the retirement precondition, at one update/day = **four calendar days** for a single attacker-controlled feedback source to retire any memory. **Date 2026-07-25.**

## D-022 Derived state gets controls: rate-bounded movement, clamp alert, divergence alarm
**Decision:** Baseline movement per update is clamped to ±10%; a clamp binding 3 consecutive updates raises an alert; a slow (30d) vs fast (24h) reference divergence >25% raises an alarm. Guardrail-flagged runs are excluded from baseline contribution. **Context:** Derived state had zero controls by design and was assigned to watchdogs — textbook boiling-frog baseline poisoning, with a successfully walked baseline reading as *positive* lift to the kill switch. This intentionally makes derived state no longer "zero gating." **Date 2026-07-25.**

## D-023 propose_memory is a provenance class that satisfies no skip, plus rate caps
**Decision:** Proposals land quarantined with `provenance.class="proposal"`; that class can never satisfy the corroboration skip or the human-verdict skip. Caps: 2 proposals/run, 50/project/day. **Context:** Proposals arrive as traces, so two proposals = two traces = the corroboration skip — agent_control had a direct two-call route to validated with attacker-chosen wording (including the exact rare-term trigger). **Date 2026-07-25.**

## D-024 Shared scan suite moves to Phase 0; inserts require proof-of-scan
**Decision:** The injection-pattern/secret/schema scan suite is a Phase 0 shared module (`core/scans`); the repository rejects any `memory_item` insert without a `ScanVerdict`. Phase 2's gate adds "seeded injection in a tool error body never reaches candidate." **Context:** The spec sequenced the scans inside the Phase 3 distiller while Tier A shipped injectable in Phase 2 — an entire phase (with a sign-off in the middle) of unscanned attacker-shaped strings entering context. Honest coverage note recorded: injection-pattern scans measure ~42–60% TPR on weak-signal memory poisoning; the scan is a cheap layer, never the reason anything is trusted. **Date 2026-07-25.**

## D-025 Erasure via per-subject crypto-shredding; trace_subject side table
**Decision:** Trace payload sections attributable to a subject are envelope-encrypted under a per-subject KEK (`subject_key`); erasure = KEK destruction, tombstoning content while the stored object stays byte-immutable and provenance pointers stay intact. `trace_subject(run_id, project_id, subject_tag)` (multi-valued) makes subjects findable. Built in Phase 0 — it cannot be retrofitted onto a plaintext archive. **Context:** The trace was simultaneously the erasure target and the audit record (incompatible as specified), trace_index had no subject column (subjects literally unfindable), and the flagship host stores audit objects under a 90-day COMPLIANCE object lock no one can bypass. **Date 2026-07-25.**

## D-026 Render-as-data reclassified: governance control, not security control
**Decision:** Keep the invariant, the exact header, and the property tests (template-only rendering); **remove render-as-data from the anti-poisoning control list**. **Context:** Delimiting is the weakest spotlighting variant — ~50% ASR reduction non-adaptive, >95% ASR under adaptive attack (Hines et al. arXiv:2403.14720; Nasr et al.). It preserves policy subordination; it does not stop a designed adversary and must not be cited as if it does. **Date 2026-07-25.**

## D-027 Kill-switch lift is stratified; arm assignment logged on the trace
**Decision:** Lift is computed on runs where something was actually injected, versus a shadow-retrieved holdout (the holdout arm runs the retriever and discards the result, so comparable runs are identifiable). Trigger: conservative lower confidence bound < 0 sustained 14 days, per-cell minimum N = 200, Benjamini–Hochberg correction. Arm assignment: salted deterministic hash, session-stable, never disables working memory or the tool cache, recorded on `trace_index.arm`. **Context:** With ≥50% abstention, aggregate on-vs-off lift is mostly noise-vs-noise; 5% split across agent-type × mem-type cells collapses N; uncorrected 95% CIs fire on ~1 cell in 20 per window by chance. **Date 2026-07-25.**

## D-028 Positioning: three novelty claims retracted with prior-art citations; no LoCoMo-derived evidence
**Decision:** §2's claims are corrected as follows. **Claim 1 (governed promotion — "nobody ships this"): refuted** — GovMem (arXiv:2607.02579) and MemClaw / Governed Shared Memory (arXiv:2606.24535) ship governed promotion/scoped writes; cite as prior art. **Claim 8 (trace-up consolidation): refuted** — Eywa (arXiv:2605.30771) preserves provenance chains to raw data to prevent recursive-summary degradation; ACE (arXiv:2510.04618, ICLR 2026) names "context collapse"; keep the invariant, drop the novelty claim. **Claim 7 (subject tags): refuted as novelty** — the data subject is a first-class Contextual Integrity parameter and CIMemories (ICLR 2026) benchmarks it; reframe as "first implementation in an agent memory store." **Claim 6 (derived state as a distinct write class) holds.** Surviving differentiators: outcome-grounded promotion between named organisational scopes, the adapter-conditioned quality lane, and the earn-your-context kill switch. **LoCoMo-derived numbers are inadmissible as evidence** — a 2026 audit found 99 score-corrupting errors in 1,540 questions and its judge accepted 62.81% of deliberately wrong answers. **Date 2026-07-25.**

## D-029 Auth: OIDC (Keycloak-compatible) + API-key mode; the service verifies its own credentials
**Decision:** Two first-class auth modes: OIDC bearer verified against a configurable JWKS (Keycloak-compatible), and hashed API keys. No unverified-header mode exists. Verdict-skip, corroboration, and operator edits are unavailable to unverified principals by construction (there are none). **Context:** The flagship host defaults to a self-asserted, unverified actor header; the human-verdict skip at weight 1.0 was otherwise forgeable by any caller who could reach the port. **Date 2026-07-25.**

## D-030 No ReMe compatibility shim
**Decision:** Tracebed ships no ReMe-compatible API. **Context:** Human decision; ReMe covers conversation-summary storage only, and the delta ("ReMe stores conversations; Tracebed learns from runs") is documented in the adapter guide instead. **Date 2026-07-25.**

## D-031 Dashboard: React 18 + Vite + TypeScript + Tailwind; ports 8110/8111
**Decision:** The dashboard is a separate Vite/React/TS/Tailwind app on :8111 (API :8110), matching Atom's frontend stack. **Context:** Views must be liftable into Atom's Command Center later; the build prompt's "server-rendered is fine" is superseded. **Date 2026-07-25.**

## D-032 "Correction" split: correction_adapter (0.8) vs operator_edit (1.0)
**Decision:** Two mechanisms, two names. `correction_adapter` (w = 0.8): inferred signal from a human editing the agent's *output*; flows through the scorer. `operator_edit`: a human editing a *memory* on the dashboard; not an adapter, bypasses the scorer, supersedes directly (a state-machine transition). **Context:** The spec used one word at two weights (0.8 in §9, 1.0 in §15), producing divergent Q trajectories per implementer. **Date 2026-07-25.**

## D-033 SDK durability contract
**Decision:** Fire-and-forget through a local ring buffer with periodic flush; drop-oldest at capacity with a loss counter; per-run monotonic sequence numbers; a `run_end` sentinel; traces with gaps or no sentinel are marked `outcome_status='incomplete'` and the distiller refuses them. Accepted loss window: buffer contents on process crash. **Context:** "Fire-and-forget" and "every run produces a complete trace" cannot both hold without a stated contract; deriving lessons from silently truncated traces produces wrong lessons with formally complete provenance. **Date 2026-07-25.**

## D-034 Migration tool: yoyo-migrations (plain SQL)
**Decision:** `yoyo-migrations` (Apache-2.0), plain-SQL migrations, plus a first-party partition manager that applies the current schema to partitions created after the migration ran. **Alternatives:** Alembic (drags SQLAlchemy against the lean-deps rule), first-party runner (reinventing ordering/locking). **Rationale:** Partitioned DDL is cleanest in raw SQL; one small dependency. **Date 2026-07-25.**

## D-035 Loosened process constraints (human-approved)
**Decision:** (a) The latency bench is built in Phase 1 and reports at every gate but is **not CI-gating from day one**; (b) test-first applies to the eight load-bearing invariants only; (c) DECISIONS.md logs deviations and dependencies, not micro-choices. The five phase STOPs remain non-negotiable. **Context:** Human decision to trade ceremony for velocity where the risk is low. **Date 2026-07-25.**

## D-036 Dependency set and licenses (initial)
**Decision:** Runtime: FastAPI (MIT), Pydantic v2 + pydantic-settings (MIT), psycopg 3 (LGPL-3.0-only — see D-005), uvicorn (BSD-3), httpx (BSD-3), yoyo-migrations (Apache-2.0), pgvector (PostgreSQL), pg_textsearch (PostgreSQL), Valkey (BSD-3), SeaweedFS (Apache-2.0), onnxruntime (MIT, secondary embedding driver), cryptography (Apache-2.0/BSD), PyJWT[crypto] (MIT). Dev: pytest (MIT), mypy (MIT), ruff (MIT). Dashboard: React 18 (MIT), Vite (MIT), TypeScript (Apache-2.0), Tailwind (MIT). Every future addition gets its own entry with license. **Date 2026-07-25.**

## D-037 Spend-ledger org rollup exempted from the aggregation ban
**Decision:** The never-build ban on cross-project aggregation is scoped to **memory content and memory-derived statistics**. Spend/token/latency metering may roll up to org for billing. **Context:** The build prompt's ban "of any kind" forbade the org billing rollup the spec mandates in three places; the wall protects content, not invoices. **Date 2026-07-25.**

## D-038 Invariant 1 scoped to learned memory; blackboard availability documented
**Decision:** "Memory is an enhancer, never a dependency" is scoped to *learned* memory (the retrieval path). Working memory and the Phase 4 blackboard are run-state with a documented availability expectation; large-payload reference-passing is therefore a stated dependency of runs that opt into it. **Context:** A pointer nobody can dereference during an outage otherwise silently violates the invariant; the contradiction had to be named, not hidden. **Date 2026-07-25.**

## D-039 Phase 0 module names reconciled to the contract; no re-export shims
**Decision:** `src/tracebed/stores/pg/models.py` -> `rows.py` and `src/tracebed/api/routes.py` -> `routes_v1.py`, renamed to the names PHASE0-CONTRACT.md §1/§5.2/§9.3 already gave them; `api/models.py`, `ingest/runner.py` and `stores/valkey/client.py` added to the §1 module map; §13.2's test-file map rewritten to the tree as built (ten of its rows named files nobody wrote — the subject matter had been merged into differently-named modules). **Context:** Four of six parallel chunks independently reported the same two name mismatches as merge blockers; a consumer following the contract literally would have failed at import. **Alternatives:** re-exporting shims at the contract names (rejected — two importable names for one module guarantees the next reader picks the wrong one, and the drift survives); amending the contract to the built names (rejected — the contract is what later work is planned against, and `routes_v1` also carries the versioning the API will need). **Rationale:** Renaming is mechanical and fully covered by the suite; a map naming files nobody wrote is worse than no map, because a reader cannot distinguish "not written" from "written elsewhere". **Date 2026-07-25.**

## D-040 `POST /admin/agents/register` is one registry transaction, not three composed calls
**Decision:** `Repo.create_agent_registration(project_id, agent_type_name, principal_kind, external_ref, key_hash)` performs the agent_type upsert, the principal insert and the agent_registration insert inside one transaction, and `AdminPort` declares that method **instead of** `create_agent_type`/`create_principal`/`register_agent`. **Context:** The route composed three independent `Repo` calls, each with its own transaction. A `DuplicateRegistration` on the third committed an orphan `agent_type` and an orphan `api_key` principal whose `key_hash` was live but whose plaintext had been returned to nobody — an undead credential deposited by every failed retry of a retryable admin call. The FK chain forces the write order, so no reordering fixes it. **Alternatives:** compensating deletes in the route (rejected — a compensation that can itself fail is not atomicity, and it runs in a different transaction from the write it undoes); keeping the three builders on `AdminPort` and documenting the hazard (rejected — an atomic implementation reachable only by convention is not atomic). **Rationale:** The registry tables are unpartitioned and share one connection path, so a single transaction is available and cheap; declaring only the atomic method on the Protocol makes the non-atomic composition unreachable from a route rather than merely discouraged. The agent_type half is `ON CONFLICT (project_id, name) DO UPDATE ... RETURNING`, which also makes §9.3's "creates agent_type if new" true rather than aspirational. **Date 2026-07-25.**

## D-041 `POST /v1/invalidation` persists synchronously; it does not get a fourth queue topic
**Decision:** The route writes one `invalidation_event` row through `Repo.insert_invalidation_event(project_id, event_type, selector)`, scoped by the authenticated principal's `ProjectScope`; `event_id` is server-generated. **Context:** PLAN.md §3 lists the route and PLAN.md §5 defines the table, but PHASE0-CONTRACT.md §9.3's route table lists neither, so the route shipped authenticating, resolving scope, returning 202 "accepted" and discarding the body — the one failure mode a 202 must never have, because "accepted" and "accepted and stored" are indistinguishable from the wire and every future integration test would have passed by accident. **Alternatives:** a fourth queue topic (rejected — §14's queue DO-NOT list forbids adding topics beyond the three constants); leaving the route a documented stub (rejected — a lie that passes tests is worse than a missing route). **Rationale:** §14's "routes 202 + enqueue — do NOT write synchronously" names trace and outcome rows, the two high-volume tables; an invalidation is a small bounded row on a low-rate webhook path, so a scoped insert is within both the letter and the intent. **Date 2026-07-25.**

## D-042 Concurrent trace-index writers serialise on a per-run advisory lock
**Decision:** `ScopedRepo.get_trace_index(run_id, *, for_update=False)`; `ingest.trace_writer` always passes `True`, which takes `pg_advisory_xact_lock` on the (project_id, run_id) pair and then `SELECT ... FOR UPDATE`. **Context:** The trace-index upsert replaces `path` wholesale — jsonb cannot be per-key merged by `ON CONFLICT` — so the writer's read-modify-write of `seq_ranges`/`payload_refs` is unsafe under concurrency. Two workers holding different batches of one run each read the pre-batch `path` and the last commit wins: a lost range can pin a complete run at `incomplete`, and a lost `payload_refs` entry orphans the only pointer to that batch's ciphertext. **Alternatives:** `FOR UPDATE` alone (insufficient — it locks nothing when the row does not exist yet, which is exactly the first-batch race); a jsonb per-key merging upsert (a larger change to frozen SQL, and it would still not order the two writers' reads); routing all batches for a run to one consumer (a partitioning scheme Phase 0 has no mechanism for). **Rationale:** The advisory lock covers both the existing-row and the not-yet-existing case, is transaction-scoped so there is no unlock path to forget, and is keyed per run so it does not serialise unrelated ingest. `for_update` defaults to `False` so plain reads stay lock-free. **Date 2026-07-25.**

## D-043 Refusals move to the wire wherever the consumer would only dead-letter
**Decision:** Three caller-supplied values are now refused by the request model rather than by the consumer: `TraceIn.seq` is capped at `ingest.trace_writer.MAX_TRACE_SEQ` (1,000,000, was int4 max); `FeedbackIn` rejects a naive `event.occurred_at`; and `Repo.get_principal_by_external_ref` takes an optional keyword `kind` so an IdP-controlled `sub` colliding with a server-minted api-key id can no longer knock both identities out of authentication. **Context:** In each case the consumer already refused the value, but only after the route had returned 202 — the caller was told "accepted" and the event was dead-lettered out of sight. `occurred_at` is the worst of the three: the column is `timestamptz`, Postgres reinterprets a naive value in the session TimeZone, and T+2-day feedback attach joins on it. **Alternatives:** importing the ingest constant into `api` (rejected — §14 keeps the request plane and the consumer plane apart; the value is mirrored and `test_integration_seams.py` asserts the two agree); adding a tz validator to `domain.events.FeedbackEvent` (rejected — `domain/` is frozen, and the wire model is the correct boundary anyway). **Rationale:** The consumer-side refusals stay in place for envelopes arriving from a replay tool rather than a route; this adds a boundary, it does not move one. **Date 2026-07-25.**

## D-044 No bare wall-clock call survives in `src/`; the one exception is named in a test
**Decision:** `SigV4Signer.sign`'s `now` parameter is required (it had defaulted to `datetime.now(UTC)`), and `S3TraceStore` takes an injectable `clock: Clock` defaulting to `SystemClock`. `tests/phase0/test_integration_seams.py` walks every file under `src/tracebed/` with an AST scan that resolves import aliases and fails on any `datetime`/`date`/`time` wall-clock call outside `domain/clock.py`. The single allowed exception — `domain/ids.py::uuid7`'s `time.time_ns()` fallback for the UUIDv7 time prefix — is listed as data in that test rather than skipped silently. **Context:** Hard rule 4 exists so the Phase 2 soak can replay; a component that reads the wall clock directly cannot be replayed, and a soak that cannot be replayed cannot tell real drift from a scheduling artefact. The sigv4 default fired on every real signed request. **Alternatives:** a grep-based check (rejected and mutation-disproved — `import datetime as _d; _d.datetime.now()` evades any literal match, and the first version of this scanner did exactly that); allowing the sigv4 default and documenting it (rejected — the point of the rule is that there is no small exception on a request path). **Rationale:** The uuid7 exception is defensible in a way the others were not: the embedded timestamp is not a business timestamp, nothing joins, buckets or expires on it, and every caller that cares passes `now_ms=clock.now_ms()`. **Date 2026-07-25.**

## D-046 Abstention's three gates share two `OutcomeCode`s; cold start reports as rarity
**Decision:** `hotpath/abstention.py` runs three independently-sufficient gates (`cos_threshold`, `bm25_norm_threshold`, the rarity gate) but reports one of only two codes: `cos_threshold` and `bm25_norm_threshold` failures both report `OutcomeCode.ABSTAINED_THRESHOLD`; the rarity-gate failure and the cold-start floor (`rarity_min_corpus_docs`) both report `OutcomeCode.ABSTAINED_RARITY`. **Context:** PLAN.md §7 describes "three gates, each ... with its own OutcomeCode", but `domain/enums.py` (frozen Phase-0 surface, PHASE0-CONTRACT.md §3.2) defines exactly two abstention codes and a third cannot be added without touching a frozen file. **Alternatives:** add `ABSTAINED_COS`/`ABSTAINED_BM25` distinct codes (rejected — requires modifying frozen `domain/enums.py`, which is a contract_gap to report, not a file to touch); encode the specific failing gate only in an internal dataclass field with no enum distinction at all (rejected — loses the threshold/rarity split the wire contract and `retrieval_event.outcome_code` do expect). **Rationale:** `ABSTAINED_THRESHOLD` and `ABSTAINED_RARITY` already name exactly this split (a raw-signal threshold miss vs. an IDF/rarity-basis failure); cold start is a rarity-basis failure (no statistical basis for an IDF judgement), not a new category. Reported as a contract_gap for whoever owns `domain/enums.py` to decide whether a third code is worth a frozen-file change; not invented as a workaround here. **Date 2026-07-26.**

## D-045 `tests/phase0/conftest.py` rebuilt; a fixture that cannot construct its object is a latent red gate
**Decision:** `pg_pool` is `create_pool(dsn)` (there is no `ScopedPool` — `stores/pg/pool.py` exposes `create_pool` and `scoped()` deliberately), with migrations applied once per session per DSN; `work_queue` passes `WorkQueue` all three required arguments; `two_projects` is implemented, provisioning two real projects and reading each scope back through `Repo.resolve_project`; `valkey_url` and `s3_config` are added, each funnelling through exactly one skip point. **Context:** As merged the first two were wrong and the third did not exist, and none of it was visible: `tests/conftest.py::pg` skips before any fixture body runs, so on a machine with no Postgres the tree was green. The moment a database became reachable, every integration test in `test_queue.py`, `test_migrations.py`, `test_partitions.py`, `test_repo_scoping.py`, `test_repo_provenance.py`, `test_telemetry.py` and `test_spend.py` would have ERRORed at setup — and a setup ERROR is indistinguishable from a real cross-project isolation failure, which is precisely the confusion §12 forbids. **Alternatives:** leaving `harness/leak_suite/fixtures.py`'s self-contained set as the only working fixtures (rejected — it covers the leak suite only, and the duplication was a symptom, not the fix). **Rationale:** `two_projects` reads the scope back through `resolve_project` rather than assembling it from the ids it just minted, so an integration test exercises the real scope-derivation path — the same one the API uses — instead of a hand-built triple that happens to hold the right UUIDs. `test_integration_seams.py` now binds the conftest's constructor calls against the real signatures statically, so this class of defect cannot recur silently. **Date 2026-07-25.**

## D-047 Working-memory offload: caller-supplied token_count; spill `first_seq` derived above the trace-writer's ceiling
**Decision:** `WorkingMemory.set()` takes an explicit `token_count: int` from the caller rather than estimating one from `value`'s bytes — `stores/valkey` has no tokenizer and must not invent a heuristic a hotpath assembler's own accounting would then have to agree with. When `token_count > session.offload_threshold_tokens`, the value is written through `TraceStorePort.put` and only a small pointer envelope occupies the Valkey key; the `first_seq` used for that `put` is `sha256(key)`-derived and placed strictly above `ingest.trace_writer.MAX_TRACE_SEQ` (mirrored here as `_TRACE_SEQ_CEILING = 1_000_000`, the same "mirror, don't import" move C-33 made for the api/ingest pair), and is stable for a given working-memory `key` so a re-`set()` of one key overwrites its own prior spill instead of leaking an orphaned object. **Context:** `TraceStorePort.put`'s object path is exactly `(project_id, run_id, first_seq)` — reusing the trace writer's own low `first_seq` values (0, 1, 2, ...) for scratch-memory spills risked a working-memory write silently overwriting a real trace-event object for the same run, corrupting the encrypted trace archive invariant 6's provenance depends on. **Alternatives:** a shared Valkey-backed counter per run (rejected — this module has no counter primitive and inventing one is key-construction territory `keys.py` owns, not this chunk); importing `MAX_TRACE_SEQ` from `ingest.trace_writer` (rejected — `stores.valkey` must not depend on `ingest`, matching the existing api/ingest precedent). **Rationale:** A hash-derived offset gives both required properties (same key → same path; different keys → different paths with a birthday bound no realistic working-memory session approaches) without a shared mutable counter or a cross-chunk import. **Date 2026-07-26.**

## D-050 Retriever chunk: pg_textsearch's SQL surface inferred; pinned excluded from dynamic arms; retriever's scope stops at the embed sub-budget
**Decision:** Five choices made to build `stores/pg/search.py` / `hotpath/fusion.py` / `hotpath/retriever.py` where PLAN.md and the contract were silent or split across chunk boundaries:
(a) **pg_textsearch's SQL surface** is nowhere specified beyond the index access-method name (`ddl.py`'s `USING bm25 (content) WITH (text_config='english')`); this chunk defines a match predicate `content @@@ %(query)s` and a scoring function `bm25_score(content, query) -> double precision` (raw, unbounded — D-003, never `ts_rank`), named after the index's own access method, and documents this as inference rather than fact in `search.py`'s module docstring.
(b) **`pinned` is excluded from every query in `search.py`**, even though it is a member of `state_machine.RETRIEVABLE_STATUSES`: PLAN.md §7 parenthesises it "(prefix only)", the state machine's own comment says the same, and `domain.enums.Slot` has no slot a pinned preference could occupy in a BM25/ANN result — only `state_machine.Status.VALIDATED` and `Status.CANDIDATE` (Tier A only) are queried by these dynamic arms.
(c) **`retrieval.hnsw_iterative_scan` (a bool in `EffectiveConfig`) maps to pgvector's three-valued `hnsw.iterative_scan` GUC** as `True -> 'relaxed_order'`, `False -> 'off'` — the config surface does not carry pgvector's finer `strict_order` distinction, and `relaxed_order` is the higher-recall iterative mode.
(d) **RRF ties (within one arm, and in the final fused sum) break by ascending `str(memory_id)`** — arbitrary but deterministic, so `hotpath/fusion.py::fuse()` always agrees with itself across repeated calls on the same input.
(e) **`Retriever.retrieve()`'s scope stops at the embed sub-budget and concurrency**, not the full degradation ladder: it catches only `EmbeddingTimeout` (-> lexical-only, `degraded=True`) and runs both arms via a persistent two-worker thread pool; the total-budget-exceeded ("static prefix only") and store-error ("nothing") rungs (PLAN.md §2 invariant 2) are left to the assembler/API layer, which this chunk's own task description and test list do not name as in scope here.
**Context:** (a)-(d) are each a genuine silence in PLAN.md/PHASE0-CONTRACT.md this chunk had to resolve to write executable SQL/logic; (e) is a scope boundary made explicit because `hotpath/abstention.py` (a sibling, already-landed chunk) and `hotpath/pipeline.py` (a third, already-landed chunk) both assume pieces of this wiring that this chunk's task description does not itself test — see this chunk's contract_gaps for the specific seam (`search.document_frequency`/`corpus_size` are implemented and exported for a future chunk to compose into `abstention.CandidateSignals.rarity`, which needs candidate content this chunk's `FusedCandidate` does not carry).
**Alternatives considered:** guessing pg_textsearch's real (if any) function names from the extension's name alone without documenting the guess (rejected — silent invention is exactly what hard rule 4 and this repo's contract-gap convention forbid); including `pinned` in the retrievable-status set for these arms (rejected — no slot exists to render it into, and it would hand the assembler rows it cannot place); having `retrieve()` also enforce the total wall-clock budget via a hard executor timeout (rejected — that rung's outcome code, `timeout_prefix_only`, requires knowledge of the static prefix this module has no visibility into; conflating "the arms took too long" with "here is the prefix instead" inside `Retriever` would require it to also own prefix lookup, which is a different chunk's file).
**Date 2026-07-26.**

## D-048 Abstention signals must be finite; the cosine domain is [-1, 1], not [0, 1]
**Decision:** `hotpath/abstention.py` and `hotpath/calibration.py` refuse non-finite raw signals at construction (`CandidateSignals.bm25_raw`, `CandidateSignals.cos_sim`, `CalibratedSignals`' four terms, `bm25_saturate`'s `raw_bm25`/`k`, `recency_weight`'s `age_days`/`half_life_days`), and `cos_sim` is accepted over `[-1, 1]` rather than `[0, 1]`. **Context:** Two defects, opposite in direction. (a) FAIL OPEN: every gate is a `<` comparison, NaN compares `False` against everything, and `bm25_raw` was validated with `self.bm25_raw < 0` — which NaN and `+inf` both pass. `bm25_saturate(nan)` and `bm25_saturate(inf)` both return NaN, so `bm25_norm < bm25_norm_threshold` was `False` and a candidate whose lexical score was a store overflow or a null INJECTED, having cleared every threshold gate; verified against the pre-fix code, `decide()` returned `inject=True, outcome_code=None` for both. (b) FAIL CLOSED: the vector arm computes `1 - (embedding <=> query)` over pgvector cosine ops, whose range is `[-1, 1]`, so a candidate pointing away from the query raised `ValueError` inside the hot path. `Pipeline` catches everything from the retrieval stage and reports `store_error`, so an ordinary abstention would have been recorded as the system failing — the exact confusion `retrieval_event.outcome_code` exists to prevent (PLAN.md §5). **Alternatives:** clamping NaN to 0.0 and injecting nothing silently (rejected — a NaN out of a store is the system failing, and the ladder already has a rung that says so; swallowing it hides the store defect forever); clamping a negative cosine to 0.0 (rejected — it is a real signal and gate 2 already rejects it correctly, so clamping would only destroy the distinction between "weakly related" and "opposed"). **Rationale:** Every range check is written `not (lo <= x <= hi)` rather than `x < lo or x > hi` precisely so NaN falls on the refusing side; `math.isfinite` covers the one-sided bounds. `bm25_saturate`'s documented codomain is corrected from `[0, 1)` to `[0, 1]`: the real-valued function never reaches 1, but `raw + k == raw` in binary floating point past roughly `k * 2**53`, so `bm25_saturate(1e308, k=10.0)` is exactly 1.0 — harmless (such a score should pass) but the old docstring's claim was false and the test only checked up to 1e6. **Date 2026-07-26.**

## D-049 Assembler `Candidate` shape invented, not pinned by any contract; CANDIDATE_NOTE is the Tier A cap's home
**Decision:** `hotpath/assembler.py` defines its own `Candidate` dataclass (`slot`, `memory_id`, `mem_type`, `text`, `tokens`, `score`, `dedup_key`) as the retriever->assembler boundary, since no `PHASE1-CONTRACT.md` exists to pin it (unlike Phase 0's binding cross-chunk contract) and `hotpath.retriever`/`hotpath.pipeline` (landed concurrently in this workspace, not owned by this chunk) declare only a `RetrievalStagesPort` seam whose `CandidateSetResult` already expects an ordered `ContextSlot` sequence -- i.e. exactly this chunk's *output* shape, not its input. Tier A's `tier_a.candidate_cap_per_run` count cap is enforced specifically on `Slot.CANDIDATE_NOTE` (not on a separate `trust_tier`/`status` field on `Candidate`), because PLAN.md section 5's own retrievable-statuses note already identifies "candidate: Tier A only, cap 1/run, labeled lower-trust" as a distinct rendering concern from `fact`/`exemplar`, and `domain.enums.Slot` already carries a dedicated `CANDIDATE_NOTE` member for it. **Context:** Building an assembler against an unpinned input shape is a genuine cross-chunk risk -- a future retriever implementation may hand over a differently-shaped record. **Alternatives:** wait for a retriever chunk to land and reverse-engineer its shape (rejected -- `hotpath.retriever` in this workspace produces per-candidate raw signals and a `calibrated_score`, not a slot-classified, dedup-keyed, packable record; no such type exists anywhere to copy); add `trust_tier`/`status` fields to `Candidate` and gate the count cap on `status == CANDIDATE` (rejected -- untested, unused complexity when the slot itself already carries that meaning by construction). **Rationale:** `renderer.render()`'s accepted type (`Sequence[ContextSlot]`, Phase 0's own frozen wire type) is the one boundary every sibling chunk already agrees on; pinning `Candidate` only at the assembler's own input edge, and reporting it as a contract_gap rather than silently asserting it as binding, keeps the disagreement visible for merge time instead of hidden. **Date 2026-07-26.**

## D-051 Working memory validates its key BEFORE spilling; `offload_threshold_tokens` is a capacity policy, not a memory guard
**Decision:** `WorkingMemory.set()` calls `keys.working_memory_key(project_id, run_id, key)` for its validation effect before any `TraceStorePort.put`, and the module docstring now states plainly that `session.offload_threshold_tokens` bounds Valkey growth only for callers honest about their own `token_count`. **Context:** `keys.py` is the only validator of `key` (type, 512-char cap, 0x1F-separator freedom) and it ran inside `working_memory_set` — i.e. *after* the spill. An over-long or separator-bearing key with an above-threshold `token_count` therefore wrote an object to the trace store and only then raised, and `TraceStorePort` has no per-object delete: caller-triggered growth reclaimable only by deleting the entire project, on an input the store layer was always going to refuse. The second half is the mirror problem: the threshold is compared against a caller-supplied integer, never against `len(value)`, so `token_count=0` writes a megabyte inline and the docstring's "growing Valkey unboundedly is the wrong trade" was an unenforced claim. **Alternatives:** deriving a byte ceiling from `offload_threshold_tokens` times a bytes-per-token factor (rejected — the factor is a literal `domain/config.py` does not define, which is exactly hard rule 4's magic number); a per-object `TraceStorePort.delete` so a late refusal could compensate (rejected — a method on a port outside this chunk's file list, and a compensation that can itself fail is not ordering). **Rationale:** Check first, work second is free here — the builder is a pure f-string, so validating twice costs nothing and keeps `keys.py` the sole owner of what a key may be. The byte-ceiling half is reported as a contract gap (`session.*` has only `idle_ttl_min` and `offload_threshold_tokens`) rather than papered over with an invented constant. **Date 2026-07-26.**

## D-052 `cache.ttl_class` durations are anchored with `\Z` and refused at zero
**Decision:** `tool_cache._DURATION` is `^(?P<qty>[0-9]+)(?P<unit>[hd])\Z`, and `resolve_ttl_class` raises for a duration resolving to zero seconds. **Context:** Python's `$` also matches immediately before a trailing newline, so `"24h\n"` — a value that arrives that way from a mounted config file or a here-doc env var — parsed under the previous `$` anchor, making the "fail loudly on a malformed duration" claim false for the most likely real malformation. `\d` likewise matches every Unicode decimal digit and `int()` accepts them, so a fullwidth numeral resolved to an ordinary TTL. `"0h"` parsed to `0`, and while `ValkeyClient._require_ttl` catches that one layer down, `resolve_ttl_class` is exported: a consumer computing a TTL here and handing it to a store without its own guard gets a key with no expiry, and the error the operator does see names `ttl_seconds` rather than the config field that is wrong. **Alternatives:** leaving the zero case to `_require_ttl` (rejected — correct only for the one caller that happens to go through `ValkeyClient`, and it points the operator at the wrong name). **Rationale:** All three are config-surface refusals with no behaviour change for any well-formed value; each is covered by a test that goes red under the exact anchor/character-class/guard mutation it exists to prevent. **Date 2026-07-26.**

## D-053 Pipeline-degradation chunk: `session_id` is an explicit keyword argument, not a `RunContext` field
**Decision:** `hotpath.pipeline.Pipeline.retrieve(scope, agent_type_id, run_ctx, *, session_id: str)` takes `session_id` as its own required keyword argument rather than reading it off `run_ctx`. **Context:** Holdout arm assignment (D-027) needs a session-stable key, but `domain.events.RunContext` (PHASE0-CONTRACT §3.5, frozen surface owned by another chunk) carries only `query_text`/`workflow_template`/`user_ref`/`tool_manifest` — `session_id` exists solely on the wire-level `RunCtxIn` model (contract §9.3), which `api/routes_v1.py` (also not this chunk's file) has not yet been updated to plumb through to a Phase 1 pipeline call at all, since Phase 0's `/v1/retrieve` was a stub that never called into holdout logic. **Alternatives:** adding a `session_id` field to `domain.events.RunContext` (rejected — not this chunk's file, and PHASE0-CONTRACT.md's frozen wire surface should not be widened without the domain-events-scan chunk's own sign-off); deriving a session key from `run_ctx.user_ref` alone (rejected — conflates "who" with "which session", and `user_ref` is optional while a holdout key must always exist). **Rationale:** Keeping `session_id` a first-class, required parameter makes the pipeline fully testable and correct against a signature it owns outright, and names the remaining wiring step (`routes_v1.py` extracting `RunCtxIn.session_id` and passing it through) as a contract_gap for whoever next touches that route, rather than silently guessing a fallback that would make holdout assignment session-stable in tests but not in production. **Date 2026-07-26.**

## D-054 Pipeline wires the real `Retriever`; a `CandidateAssemblyPort` seam covers the one genuinely unbuilt piece
**Decision:** `Pipeline` depends on `HybridRetrieverPort` (an exact structural mirror of the already-landed `hotpath.retriever.Retriever.retrieve()`, itself already handling the embed sub-budget, BM25+ANN search, and RRF fusion) for retrieval, and on an injected `CandidateAssemblyPort` for turning the resulting `FusedCandidate` sequence into a decided outcome code and an ordered `ContextSlot` list. **Context:** `hotpath.retriever`, `hotpath.abstention`, `hotpath.calibration`, `hotpath.assembler`, and `hotpath.renderer` all landed in this workspace concurrently with this chunk (no `PHASE1-CONTRACT.md` coordinates them, unlike Phase 0). `Retriever.retrieve()`'s own module docstring says its scope stops at the embed sub-budget and concurrency (D-050(e)); `hotpath.fusion.FusedCandidate` carries only `memory_id`/`trust_tier`/`status`/raw per-arm signals, never the memory's content, `mem_type`, token count, or a calibrated score, so turning it into `hotpath.assembler.Candidate` requires fetching the underlying `memory_item` row and its rarity evidence (`stores.pg.search.SearchStore.document_frequency`/`corpus_size`) — a fetch-and-compose step `retriever.py`'s own docstring explicitly leaves to "whichever component already holds candidate content," and no such component exists anywhere in this codebase. **Alternatives:** defining a single monolithic `RetrievalStagesPort` seam covering retrieval+abstention+assembly+rendering as one opaque call (the chunk's first draft; superseded — it would have thrown away the already-built, already-tested `Retriever`/`renderer.render` for no reason, and hidden the one real gap behind a much larger one); waiting for the content-fetch component to land before writing any pipeline code (rejected — the orchestrator, ladder, and holdout plumbing are this chunk's actual deliverable and are fully testable today against the seam). **Rationale:** Using the real, already-built `Retriever` directly means the first ladder rung (query-embed timeout -> lexical-only, `degraded_lexical`) is exercised against real embed-degradation semantics, not a reimplementation of them; the narrower `CandidateAssemblyPort` seam names exactly the one missing integration point instead of a broader one. **Date 2026-07-26.**

## D-055 `TelemetryRecorderPort` and `RetrievalOutcomeLike` are declared locally, not imported, to avoid contributing to a `purity_check.py` false positive
**Decision:** `hotpath/pipeline.py` declares its own `TelemetryRecorderPort` (mirroring `adapters.ports.TelemetryPort`) and `RetrievalOutcomeLike` (mirroring `hotpath.retriever.RetrievalOutcome`, via read-only `@property` members so a frozen dataclass can satisfy it) rather than importing either. **Context:** `adapters/ports.py` carries a `TYPE_CHECKING`-only import of `adapters.identity.Principal` for an unrelated Protocol in that same module (needed so `ports.py` can type-hint `PrincipalPort.authenticate`'s return value without a runtime dependency on the API layer). `scripts/purity_check.py`'s AST-based import-graph walk does not special-case `TYPE_CHECKING` blocks, so it counts `adapters.identity` as reachable from *any* module that imports `adapters.ports` at all — including, transitively, `hotpath.retriever` (which needs the real `EmbeddingPort` from `adapters.ports`), and therefore anything that imports `hotpath.retriever` too. Verified directly: scoping `purity_check.py`'s own graph-walk functions to just `tracebed.hotpath.pipeline`/`budget`/`holdout` (this chunk's three files) reports zero violations for all three; running the full `scripts/purity_check.py` over the whole `hotpath/` tree still fails, but with exactly one violation, rooted at `hotpath.retriever` alone — a file not in this chunk's list and not touched by it. **Alternatives:** importing `TelemetryPort`/`RetrievalOutcome` directly and accepting the reported violation as this chunk's own (rejected — it would make a real, fixable-by-this-chunk problem indistinguishable from the pre-existing one, and the false positive is fixable with zero behavioural change here); fixing `adapters/ports.py` or `scripts/purity_check.py` directly (rejected — neither file is in this chunk's file list; §14/hard rule 6 name this a contract_gap, not a file to touch). **Rationale:** Structural Protocol typing makes the mirrored declarations exact, zero-cost substitutes for the real types at every call site that matters to this chunk, and the AST-level verification above is the evidence that this chunk's own three files are purity-clean; the one remaining violation is `hotpath/retriever.py`'s to fix (most likely: teaching `purity_check.py`'s `imports_of()` to skip nodes nested under `if TYPE_CHECKING:`, mirroring what `from __future__ import annotations` already does for runtime safety) and is reported as a contract_gap against `scripts/purity_check.py` and `adapters/ports.py`, not silently worked around by weakening this chunk's own dependency graph. **Date 2026-07-26.**

## D-056 Assembler/renderer audit: `total_tokens` is the outer bound; an empty assembly renders nothing; non-finite scores are refused
**Decision:** Four behavioural changes to the `assembler-renderer` chunk, each made because the previous behaviour was wrong rather than merely unspecified.
(a) **`budget.total_tokens` clamps every pool.** `assemble()` now derives its static pool as `min(budget.static_prefix, budget.total_tokens)` and its dynamic pool as `min(budget.dynamic, budget.total_tokens - static_used)`. Previously the two section budgets were spent independently and an `AssertionError` — commented "structurally unreachable" — guarded the sum. It was reachable: `budget.*` is per-project and per-agent-type overridable (`OVERRIDABLE_SECTIONS`) and nothing validates that `static_prefix + dynamic <= total_tokens`, so an operator lowering only `total_tokens` got a `500` out of the hot path instead of a smaller block.
(b) **`render([])` returns `rendered=""`, not a lone `MEMORY_HEADER`.** `Pipeline._ladder` calls `render(assembled.slots)` on the abstention and empty-result rungs too, where the slot list is empty. A bare header is not "nothing" (PLAN.md §2 invariant 2): it spends prompt tokens telling the model memory was recalled when none was — the exact false-confidence signal abstention exists to suppress — and it diverged from `domain.events.empty_context_block()`, which the degradation rungs of the same function already use with `rendered=""`.
(c) **`ContextBlock.slots` carries the rendered order.** `render()` regrouped into `SLOT_ORDER` for `rendered` but stored the caller's raw order in `slots`. `injection_log.slot` rows come from `slots` while the model saw `rendered`; an auditor reconstructing the prompt from the structured view would have reported an order the prompt never contained.
(d) **`Candidate.__post_init__` refuses a non-finite `score`.** NaN compares `False` against everything, so `sorted(key=(-score, memory_id))` stops being a total order: measured, one NaN among three candidates produced four distinct packings depending only on the retriever's iteration order, breaking the module's stated determinism and the reproducibility of a rendered block from its stored `retrieval_event`. Mirrors D-048's identical refusal one layer up in `hotpath/abstention.py`.
Two defensive additions with no behaviour change under the shipped defaults: `SLOT_ORDER` is checked at import time for totality over `Slot` and for duplicates (it drives both `renderer._render_text` and `assembler._DYNAMIC_SLOTS`, so a member missing from it was a memory item silently never rendered *and* never reported as dropped), and `budget.slot_caps` is read with `.get(slot.value, 0)` because a project override may legally replace the whole mapping and omit a slot — a `KeyError` there would be an unhandled failure on a path invariant 2 requires to degrade, not fail.
**Context:** `dropped_memory_ids` is now derived by subtracting the selected set from the input set rather than accumulated inside `_dedup`/`_pack`, which is what makes (a)'s and the `slot_caps` case's exclusions observable instead of silent — an accumulate-as-you-go scheme reports nothing at all for a candidate that never reaches a `_pack` call.
**Alternatives:** validating `static_prefix + dynamic == total_tokens` in `BudgetConfig` (rejected — `domain/config.py` is outside this chunk's file list; clamping at the consumer is the fix available here and is correct regardless); clamping a NaN score to `0.0` (rejected — same reasoning as D-048: a NaN out of the scorer is the system failing, and swallowing it hides that forever).
**Rationale:** Every one of these is covered by a test that goes red under the exact mutation it exists to prevent; the mutations were run and the reds observed, not assumed. **Date 2026-07-26.**

## D-057 Retriever audit: the purity gate's last violation is fixed at its root, `fused_top_n` is applied, and invariant 7 is enforced on the way out
**Decision:** Five changes to the `retriever` chunk (`hotpath/retriever.py`, `hotpath/fusion.py`, `stores/pg/search.py`), each because the previous behaviour was wrong, not merely unspecified.
(a) **`hotpath/retriever.py` no longer imports `adapters.ports`.** It declares a local, `@runtime_checkable` `QueryEmbedderPort` Protocol carrying exactly the one `embed()` method it calls. This was the single remaining `purity_check.py` violation in the whole tree (`hotpath.retriever -> adapters.ports -> adapters.identity`), and D-055 correctly identified it as this file's to fix: `adapters/ports.py` and `scripts/purity_check.py` are both outside this chunk's file list, so the fix available here is to not create the edge. Invariant 1 is CI-blocking from this phase; a gate that fails is not a gate. `tests/phase1/test_retriever.py::test_local_query_embedder_port_matches_the_real_embedding_port` compares `inspect.signature` of the local and real `embed` so the copy cannot drift. The underlying contract_gap stands and is reported: `purity_check.py`'s `imports_of()` should skip nodes nested under `if TYPE_CHECKING:`, since those imports never execute and therefore cannot reach a generative client at runtime.
(b) **`retrieval.fused_top_n` is applied.** It was a live field of `RetrievalConfig` (PLAN.md §6, default 20) that no code in the repository read: fusion returned the entire union of both arms, up to `2 * arm_top_n` (100) candidates, straight into the assembler. `retrieve()` now cuts the fused list to `cfg.fused_top_n`, clamped at zero so a non-positive override cannot become a negative slice bound — which would mean "all but the last |n|", the exact opposite of a cap. `candidates_considered` deliberately still reports the full pre-fusion union (contract §5.2 measures work done, not survivors).
(c) **The lexical arm is submitted BEFORE the embed call, not after.** `embed_timeout_ms` (200) is two thirds of `total_budget_ms` (300), so an embed that runs to its timeout with the lexical arm not yet started spent 200ms and only then began the one arm still able to answer: the `degraded_lexical` rung would routinely blow the total budget and be demoted a second time by the ladder to `timeout_prefix_only`. Overlapping the two pays the lexical query's latency inside the embed window. Proven by a test whose fake embedder refuses to return until the lexical arm has actually started.
(d) **`stores/pg/search._row_to_arm_hit` enforces the retrievability predicate on returned rows.** Every other guarantee in that module is a property of query TEXT; a future edit (a new arm, a UNION, a hand-written variant missing one conjunct) can change it with no test noticing until a quarantined row is in a prompt. A row that is not `validated`, or a `candidate` row not at Tier A, now raises `TracebedError` instead of becoming an `ArmHit` — the message carries ids and enum values only, never `content`. Failing closed costs one retrieval (the ladder's store-error rung; invariant 2 is preserved, the run does not fail); failing open costs the entire quarantine guarantee. `_RETRIEVABLE_STATUS_VALUES` is the single source of truth for both the bound query parameter and this check, so the query and its post-condition cannot disagree.
(e) **`document_frequency` bounds its term list (`_MAX_DF_TERMS`, 512).** `terms` is derived from caller-supplied query text and its sibling `top_n` was already clamped; unbounded, one pathological query becomes an unbounded `text[]` parameter and an unbounded server-side `unnest`. Truncation rather than refusal because the rarity gate needs only `abstention.rarity_min_shared_terms` (2) rare shared terms — dropping the tail of an absurd term list can only make the gate stricter, never flip an abstain into an inject. Same class of bound as `_MAX_ARM_TOP_N`: a DoS ceiling, never a retrieval-quality tunable, and reported as a contract_gap rather than treated as a config value.
**Also fixed:** `tests/phase1/test_fusion.py::test_smaller_rrf_k_amplifies_a_rank_one_advantage` could not fail — it recomputed the RRF formula by hand and only asserted that `fuse()` returned something, so it stayed green against a `fuse()` that ignored `rrf_k` entirely (verified by running the original test body against exactly that mutant). Replaced with a test asserting `fuse()`'s own output ORDER across the `k=2` crossover of `1/(k+1) = 2/(k+4)`, where a rank-1-in-one-arm candidate and a rank-4-in-both-arms candidate genuinely swap places between `rrf_k=1` and `rrf_k=60`.
**Alternatives:** applying `fused_top_n` in the assembler instead (rejected — the assembler would then do per-candidate content fetch and tokenisation for up to 100 rows before cutting to 20, which is the cost the knob exists to avoid); making the retrievability check a `logger.warning` (rejected — invariant 7 has no "log and continue" mode); editing `adapters/ports.py` or `scripts/purity_check.py` to fix (a) at its true root (rejected — hard rule 6, neither is in this chunk's file list).
**Rationale:** every change above is covered by a test that was observed going red under the exact mutation it exists to prevent — the cut removed, the clamp removed, the submit reordered, the guard deleted, the predicate's `OR` turned into `AND`, `scoped()` swapped for a bare connection, the term bound dropped, and `rrf_k` hardcoded. **Date 2026-07-26.**

## D-058 The retrieval deadline is anchored at call entry, and the embed sub-budget is clamped to what remains of it
**Decision:** `hotpath.budget.Deadline` accepts an explicit `started_at_ms` anchor, and `hotpath.pipeline.Pipeline` passes the `clock.monotonic_ms()` reading it took on entry to `retrieve()`; `Pipeline._embed_bounded` then narrows `retrieval.embed_timeout_ms` to `min(embed_timeout_ms, remaining)` before handing the config to the retriever. **Context:** the audited implementation constructed the `Deadline` inside `_run_ladder`, i.e. AFTER config resolution and holdout assignment. Two consequences, both real: (a) `ConfigResolver.effective()` reads `project_config` / `agent_type_config`, so it can stall — and a stall there was excluded from the very budget it blew, so a 350ms config stall produced a full search and an `outcome_code=injected` row with a >300ms wall time; (b) the ladder's first `total_exceeded()` check (before the retriever) was provably dead code, since elapsed time at ladder entry was always 0 and the constructor rejects a non-positive budget. The embed clamp is the same defect one level down: PLAN.md §6's two budgets are nested, not independent, so an embed call handed its full 200ms with 40ms of the total left overruns the budget the caller is actually waiting on, detectable only afterwards. **Alternatives:** leaving the anchor and treating the pre-ladder latency as negligible (rejected — it is a store round trip, which is exactly the thing that stalls); passing a remaining-time float into `Retriever.retrieve()` as a new parameter (rejected — `retriever.py` is another chunk's file, and `RetrievalConfig` already carries the field, so `model_copy` narrows it with no signature change anywhere). **Rationale:** invariant 2's budget is the *call's* budget. Both numbers still come from `EffectiveConfig`; the clamp only ever narrows, and it is reached only after `total_exceeded()` was false, so the clamped value is at least 1ms and can never reach `EmbeddingPort.embed` as a degenerate `timeout_ms=0`. Proven by mutation: reverting the anchor, ignoring the supplied anchor, removing the clamp, and widening `Deadline.total_exceeded()` by one tick each turn a test red. **Date 2026-07-26.**

## D-059 `Pipeline.retrieve()` takes no `agent_type_id`: it is read from the server-derived `ProjectScope`
**Decision:** the `agent_type_id` parameter is removed from `Pipeline.retrieve()`, from `CandidateAssemblyPort.run()`, and from `StaticPrefixPort.get()`; all three read `scope.agent_type_id`. **Context:** `/v1/retrieve`'s request body carries `agent_type: str` (PLAN.md §3), while the authoritative agent type comes from the `agent_registration` row `Repo.resolve_project()` reads (`UNIQUE(principal_id)` — one principal resolves to exactly one agent type). A pipeline that accepts an agent_type_id alongside the scope is a channel for the caller-asserted one to arrive: agent-type-scoped memories, the per-agent-type static prefix key, and the per-agent-type config overlay would all be selectable by whatever the caller wrote in the body. **Alternatives:** keeping the parameter and asserting it equals `scope.agent_type_id` (rejected — a check that exists only when someone remembers to call it, versus a channel that does not exist at all); keeping it for symmetry with `scope` (rejected — that symmetry IS the defect). **Rationale:** invariant 4 says scope is derived server-side and never caller-asserted, and `routes_v1.py` already applies that one dimension in — `_trace_envelope` stamps `str(scope.agent_type_id)`, never `body.agent_type`. This makes the hot path match. **Date 2026-07-26.**

## D-060 A session-less run is its own session; `assign_arm` refuses an empty session key
**Decision:** `Pipeline.retrieve(..., session_id: str | None = None)` substitutes the minted `run_id` when `session_id` is absent or blank, and `holdout.assign_arm` raises on an empty `session_key`. **Context:** the audited signature required `session_id: str`, but the wire field is `api.models.RunCtxIn.session_id: str | None = None`, so the wiring step would have had to pass `None` or a placeholder. Hashing a shared placeholder puts EVERY session-less run of one agent_type into a single draw: at `killswitch.holdout_pct = 5` that is a 5% chance that 100% of that traffic sits in the holdout arm (memory withheld wholesale once Phase 3 acts on the assignment) and a 95% chance that none of it does (lift measured on an empty cell). **Alternatives:** deriving a key from `run_ctx.user_ref` (rejected — optional, and conflates "who" with "which session"); leaving `session_id` required and pushing the choice onto the route (rejected — that is how the placeholder gets invented, silently, downstream). **Rationale:** a run with no session is its own session: still deterministic for that run, still uniform across runs, and D-027's session-stability claim is unaffected because there is no session to be stable across. The `assign_arm` guard makes the degenerate input loud instead of plausible. **Date 2026-07-26.**

## D-061 `retrieve()` guards the clock, the run-id mint, and the response-model construction, not only its dependencies
**Decision:** `Pipeline` wraps `clock.monotonic_ms()`, `clock.now_ms()`-seeded run-id minting, the latency read, and the `RetrieveResult` construction, and type-checks the `StaticPrefixPort` return value before it reaches the response model. A monotonic-clock failure degrades to `store_error` (nothing can be budgeted, and unbudgeted work on the synchronous path is what invariant 2 forbids); a wall-clock failure falls back to `ids.uuid7`'s own time source. **Context:** the audited implementation guarded every dependency call but left four statements outside every `try`. The reachable one: `StaticPrefixPort` has no implementation until Phase 2's `prefix_builder`, so its return value is unvalidated data read back out of Valkey — `_timeout_prefix_only` caught the *call* raising but not the *value* being wrong, and a wrong-typed value reaches the Pydantic response model from outside every ladder guard. `Clock` is likewise an injected Protocol, i.e. third-party code on the hot path. **Alternatives:** one outer `try` around the whole method (rejected — it would swallow the outcome the ladder had already computed and report `store_error` for a call that had genuinely retrieved something); trusting the prefix port's declared return type (rejected — that is the definition of unvalidated). **Rationale:** "a run never blocks or fails because of Tracebed" is a claim about `retrieve()`'s boundary, not about its dependencies' boundaries. The one boundary this class still does not own is `mint_run_id()` failing outright (`os.urandom` failing), which is exactly what `run_id_origin="sdk"` exists for (D-018 / C-26). Proven by mutation: removing each guard, and trusting the prefix value, each turn a test red — including a case where the prefix returns a mapping that relabels the block, which Pydantic would otherwise coerce and which invariant 3 forbids. **Date 2026-07-26.**

## D-062 Phase 1 gate harness: negative probes exercise real production code via a harness-local `CandidateAssemblyPort`; the fail-open drill adds a config-store scenario; the bench documents two Repo gaps it does not paper over
**Decision:** Four choices made building `harness/negative_probes/`, `harness/failopen_drill.py`, and `harness/latency_bench.py`.
(a) **`harness/negative_probes/probes.py::ProbeAssembly`** is a genuine, correct `hotpath.pipeline.CandidateAssemblyPort` implementation — it calls the real, already-tested `hotpath.abstention.decide`, `hotpath.calibration.calibrated_score`, and `hotpath.assembler.assemble` over each probe's synthetic candidate content, and `run_probe_through_pipeline` wires the same object into a real `hotpath.pipeline.Pipeline`. Nothing about the *decision* is faked, only where a candidate's raw signals and content come from (a fixture, not a live `SearchStore`) — no `CandidateAssemblyPort` implementation exists anywhere in the production codebase yet to fetch that from a real row (D-054). A `positive_control_probe()` (excluded from the 25+ negative-probe count) proves the harness is not rigged to always abstain: it clears every gate and does inject, through the identical wiring.
(b) **32 probes across the four PLAN.md §7 classes** (empty vault, uncovered query, generic-common-terms, cold-start; 8 each), each engineered so exactly one gate is the reason for abstention (e.g. the generic-common-terms fixtures deliberately clear the cosine/BM25 thresholds so only the rarity gate's IDF computation can be what refuses, isolating it from the cold-start branch it shares an `OutcomeCode` with per D-046).
(c) **`harness/failopen_drill.py` adds a sixth scenario, `config_store_down`**, beyond the four PLAN.md §2 names literally ("kill Postgres, kill Valkey, stall the embedder, stall everything"): `ConfigResolver.effective()` is itself a `project_config`/`agent_type_config` Postgres read, and a stalled/dead config store is a real, distinct way "Postgres is down" can present before the ladder even starts — `test_degradation_ladder.py`'s own `test_config_resolution_failure_also_returns_store_error` covers the same case at the unit level, so this is drilled here too rather than left uncovered. A `valkey_down_at_prefix` scenario combines an already-blown total budget with a raising `StaticPrefixPort`, and `everything_down_at_once` combines a raising retriever AND a raising static prefix simultaneously — the compound worst case, still `store_error`, still completing.
(d) **`harness/latency_bench.py` discovers, and does not silently route around, two real `Repo` gaps**: `Repo.insert_memory_item`'s INSERT statement never writes `memory_item.embedding`/`embedding_model_id`/`embedding_model_version`/`lexemes` (verified by reading `stores/pg/repo.py` — no method anywhere writes those four columns), so `SearchStore.vector_arm`'s `embedding IS NOT NULL` predicate matches zero rows for any data seeded through the sanctioned `Repo` path, in this bench and in production alike; and `Repo` has no bulk-insert primitive, so seeding at the real 50 x 100,000-item scale goes through 5,000,000 individual governed `Repo.insert_memory_item` calls (parallelised across `--seed-workers` threads, still not a `COPY`). Both are reported loudly in the module's own docstring and in every `LatencyBenchReport.known_gaps`, not hidden behind a report that looks like a real vector-arm bench.
**Alternatives considered:** letting the negative probes call `hotpath.abstention.decide` directly with no `Pipeline`/assembly wiring at all (rejected — proves the gate math but not that the orchestrator actually reaches it, which is what `run_probe_through_pipeline` adds for free once `ProbeAssembly` exists); inventing a `Repo`-bypassing raw-SQL seeding path for the bench to work around the missing embedding-write method (rejected outright — hard rule 2/`scripts/raw_sql_lint.py` ban all SQL outside `stores/pg/`, and a harness script is not an exception); silently reporting the vector arm's zero-row latency as if it were a real ANN bench (rejected — indistinguishable from a passing vector-arm bench to a reader who has not read this decision, which is the exact species of lie a gate report must not tell).
**Rationale:** every negative probe and every fail-open scenario is a real, runnable assertion today (`python harness/negative_probes/probes.py`, `python harness/failopen_drill.py`, both exit 0); the bench's two gaps are handed to whoever next touches `stores/pg/repo.py` as concrete, reproducible findings rather than as a vague "seeding was slow" note. **Date 2026-07-26.**

## D-063 `/v1/retrieve` calls the real `Pipeline` only when `AppDeps` supplies one; `AppDeps` cannot be extended from this chunk's file list
**Decision:** `api/routes_v1.py::retrieve()` now does `pipeline = _pipeline_dep(deps)` (a `getattr(deps, "pipeline", None)` check against a locally-declared `PipelinePort` Protocol mirroring `hotpath.pipeline.Pipeline.retrieve`'s exact signature) and calls it for real when present; when absent, the handler falls back to byte-for-byte the same Phase 0 stub it replaced (mint a UUIDv7 `run_id`, arm `memory_on`, `outcome_code=empty_result`, empty context block, one `telemetry.record_retrieval` call). **Context:** this task's file list is `api/routes.py`/`routes_v1.py` ONLY for the `api/` directory (hard rule 6) — `api/deps.py::AppDeps` (a `@dataclass(slots=True)` with fields `verifier`/`resolver`/`queue`/`telemetry`/`memory_reader`/`exporter`/`invalidations`/`admin`/`partitions`/`keys`/`clock`, verified by reading the file directly) has no field a real `Pipeline` could be read from, and constructing one requires THREE things `AppDeps` does not carry at all: a `ConfigProvider`/`domain.config.ConfigResolver` (needs a `ConfigStorePort`, i.e. `Repo`, which `AppDeps` also does not expose), a `HybridRetrieverPort`/`hotpath.retriever.Retriever` (needs a `SearchStore` and an `EmbeddingPort`, neither present either), and a `CandidateAssemblyPort` — which, per D-054/D-062(a), has **no implementation anywhere in the production codebase**, only the harness-local one this same chunk built for testing. Constructing a `Pipeline` from inside `routes_v1.py` by reaching past `AppDeps` (e.g. building a fresh `Repo`/`SearchStore` from raw settings on every request) would duplicate `api/main.py::create_app`'s entire dependency-wiring responsibility inside a route handler, and a hand-rolled `CandidateAssemblyPort` written here — with nothing to fetch real candidate content from — would be exactly the "function returning a plausible constant instead of computing" hard rule 7 forbids. **Alternatives considered:** adding a `pipeline` field to `AppDeps` and wiring it in `api/main.py` (the correct eventual fix — rejected HERE only because both files are outside this chunk's file list, per hard rule 6, and is named explicitly so the next chunk to touch either file does not have to rediscover it); leaving `/v1/retrieve` as the untouched Phase 0 stub (rejected — the task explicitly asks for the real call to be wired, and the `getattr`-based seam makes that literally true the moment the dependency exists, with zero further change to this file); constructing a `Pipeline` per-request from ad-hoc settings reads inside the route (rejected — reintroduces exactly the untestable-offline, DI-bypassing shape `AppDeps` exists to prevent, and still cannot supply a real `CandidateAssemblyPort`). **Rationale:** the pattern mirrors `hotpath.pipeline.Pipeline`'s own `static_prefix: StaticPrefixPort | None = None` seam for its analogous not-yet-built Phase 2 dependency — an absent dependency degrades cleanly rather than being faked, and `tests/phase0/test_api_scope.py`'s full suite (23 tests, all offline, none touching `deps.pipeline`) still passes unchanged, proving the fallback path is exactly as it was. **Date 2026-07-26.**

## D-064 `scripts/purity_check.py` skips `if TYPE_CHECKING:` bodies
**Decision:** The purity gate's AST walk no longer counts imports nested under `if TYPE_CHECKING:` (or `typing.TYPE_CHECKING`) as reachability edges; the `else:` branch of such a guard, and every function-local import, still count. Two new self-test assertions pin both halves. **Context:** `adapters/ports.py` names `adapters.identity.Principal` under that guard purely for an annotation. `adapters.ports` is on the gate's `ALLOWED_INTERNAL` list and `adapters.identity` is not, so the walk reported `hotpath.X -> adapters.ports -> adapters.identity` as a violation for an import that never executes. Four Phase 1 modules independently declared duplicate local Protocols to route around it (D-055, D-057, and `hotpath/pipeline.py`'s `TelemetryRecorderPort`/`RetrievalOutcomeLike`), producing the strange steady state where NO hot-path module could import the one adapters module the gate explicitly permits. The gate was green only because everyone had routed around it. **Alternatives:** adding `tracebed.adapters.identity` to `ALLOWED_INTERNAL` (rejected — it widens the allowlist for a module the hot path genuinely must not reach at runtime, trading a false positive for a real hole); moving `Principal` into `domain/` (rejected — it is an authentication type with an adapters-shaped lifecycle, and relocating frozen Phase 0 surface to satisfy a linter is the tail wagging the dog); leaving it (rejected — a gate that is red for a reason no edit to the audited package can fix stops being read, and invariant 1 is CI-blocking from this phase). **Rationale:** A `TYPE_CHECKING` import emits no runtime import, so it cannot reach a generative client; counting it is a strictly false edge. The narrow recogniser (bare `TYPE_CHECKING` / `x.TYPE_CHECKING` only) keeps any unfamiliar guard being walked, because over-approximating reachability is a false red and under-approximating it is a hole — only one of those is safe to get wrong. The duplicate Protocols were left in place with corrected docstrings: each now stands on the reason that outlives the false positive (a narrower surface for offline fakes), and each has a signature-drift test. **Date 2026-07-26.**

## D-065 A gate whose arm produced no signal is SKIPPED, not failed
**Decision:** `abstention.CandidateSignals.cos_sim` and `.bm25_raw` are `float | None`; `decide()` skips the cosine gate when there is no cosine and the BM25 gate when there is no BM25, and refuses a candidate carrying neither. `AbstentionDecision.bm25_norm` is correspondingly `float | None`. The rarity gate is unaffected — it reads no arm score, so it applies in full on every rung. **Context:** `hotpath.fusion.FusedCandidate` carries `ArmSignal | None` per arm ("a candidate absent from an arm carries None rather than a zero score — zero is a real score"), but `CandidateSignals` required a `float` for both. The abstention chunk flagged this as the one modelling question it would not decide unilaterally, because the two readings have opposite consequences and neither is a measurement. **Alternatives:** absent ⇒ that gate fails (rejected — on the ladder's `degraded_lexical` rung NO candidate has a cosine, because the vector arm never ran, so a rung PLAN.md §2 defines as a WORKING degradation would return nothing on every call forever, while every dashboard showed a healthy service); absent ⇒ substitute 0.0 (rejected — silently abstains everything, same failure with a number attached); absent ⇒ substitute 1.0 (rejected — silently injects on evidence nobody gathered). **Rationale:** A gate is a test applied to evidence; with no evidence there is nothing to test. This does mean a lexical-only candidate is not cosine-checked — that is the honest cost of the degraded rung, and it is bounded: the rarity gate (the strongest of the three, and the one that stops a generic query matching generic memory) plus the arm that DID answer still apply. Separately, `calibration` still needs a number for its similarity term, and there 0.0 is used deliberately — as the additive identity of that term, documented at the call site — because the score's job is to ORDER candidates against each other and re-normalising weights per-candidate would put two candidates in one list on two different rulers. **Date 2026-07-26.**

## D-066 `hotpath.assembly` tokenises with its own word splitter, not pg_textsearch's
**Decision:** "Shared term" is computed by a pure `[0-9a-z]+` splitter applied to both the query and the candidate's content; per-term document frequency is still computed by the server (`content @@@ term`) under the real tokenizer. **Context:** The rarity gate needs the query terms a candidate actually shares, which requires tokenising candidate content — and pg_textsearch's tokenizer (stemming, stopwords, its `text_config='english'`) is not reachable from a pure function on the hot path. **Alternatives:** a per-candidate `to_tsvector` round trip (rejected — one statement per candidate on a 300ms budget, which is the shape the whole assembly design exists to avoid); shipping a Python stemmer (rejected — a new dependency, and D-036 keeps the dependency set closed). **Rationale:** The two tokenizers disagree only on inflection, and the disagreement is one-directional: a query term and a candidate term differing only by suffix are NOT counted as shared here even though the index would match them, so the gate sees fewer rare shared terms than a stemming tokenizer would. `rarity_min_shared_terms` is a `>=` bar, so undercounting can only turn an inject into an abstain — never the reverse. The df numbers themselves stay authoritative because the server computes them. **Date 2026-07-26.**

## D-067 The score's validity term is the row's stored `confidence`
**Decision:** `CalibratedSignals.validity` is filled from `memory_item.confidence`. **Context:** `calibration.py` documents validity as "a validity term in [0, 1] (e.g. lower for a Tier-A `candidate` row than for `validated`) — computed upstream", and PLAN.md §6's `score.*` section has `w_validity` but no field from which a per-status or per-tier validity could be derived. **Alternatives:** a status/tier table in code (rejected — every entry would be a literal threshold `domain/config.py` does not define, which is hard rule 4's exact prohibition); inventing `score.validity_validated` / `validity_candidate` config fields (rejected — inventing config surface is the same violation one layer up, and PLAN.md §6 is the authority on what fields exist). **Rationale:** `confidence` is already a per-row `[0, 1]` number whose whole purpose is "how much do we believe this", it needs no new surface, and it moves with the row rather than with a static table. Reported as a contract gap: if the intent was for validity to encode trust tier or status, PLAN.md §6 needs the fields, and `hotpath/assembly.py` is the one call site to change. **Date 2026-07-26.**

## D-068 `injection_log` is written by the pipeline, from rows the assembly produces
**Decision:** `hotpath.assembly.CandidateAssembly` returns `stores.pg.rows.InjectionRow` objects for the memories that actually reached a slot, and `Pipeline` writes them through a new optional `InjectionRecorderPort` (satisfied structurally by the existing `stores.pg.telemetry.Telemetry.record_injections`). Nothing is written when nothing was placed. **Context:** `injection_log` has existed in the schema since Phase 0 and `Telemetry.record_injections` since Phase 0, and no code anywhere called it — so the Phase 1 "Injections view" had no rows to show, and Phase 3's Recall & Rollback forensics (which enumerates a poisoned memory's blast radius through `injection_log`) had nothing to enumerate. **Alternatives:** reconstructing the rows in `Pipeline` from the rendered `ContextBlock` (rejected — `ContextSlot.memory_id` is optional and carries no score, so `injection_log.score` could only ever be fabricated); adding `record_injections` to `adapters.ports.TelemetryPort` (rejected — that is frozen Phase 0 surface; a separate optional port gets the row written without editing it). **Rationale:** The score that won a memory its slot exists only inside assembly, so assembly is the only place the row can be built honestly. The port is optional and its call is individually guarded: a deployment with no `injection_log` writer degrades to no forensics, never to a failed retrieve (invariant 2). **Date 2026-07-26.**

## D-069 The total budget is checked a third time, after assembly
**Decision:** `Pipeline._run_ladder` re-checks `deadline.total_exceeded()` after `CandidateAssemblyPort.run` returns, degrading to `timeout_prefix_only`. **Context:** The ladder checked the budget before the retriever and after it. `hotpath.assembly` then issues three more store round trips of its own — candidate content, per-term document frequency, corpus size — which neither earlier check could have covered, because that work did not exist yet. **Alternatives:** passing the deadline into `CandidateAssemblyPort.run` so it could pre-empt itself (rejected for now — it widens a seam every fake implements, and the honest pre-emption it would enable needs a `statement_timeout` derived from the remaining deadline, which is a `stores.pg` change; recorded as remaining work instead). **Rationale:** Without the third check a call answers `injected` at 400ms while writing a `retrieval_event` that says the budget held — the latency histogram that is supposed to correct `total_budget_ms` would be reading its own blind spot. The check reports correctly; it does not pre-empt, and that limitation is stated in `hotpath/assembly.py`'s module docstring rather than hidden. **Date 2026-07-26.**

## D-070 Invariant 7 is re-asserted on the last hop before the renderer
**Decision:** `stores.pg.search`'s retrievability post-condition is exported as `assert_dynamically_retrievable(memory_id, status, trust_tier)` and called a third time by `hotpath.assembly`, on every fetched row, before it can become an `assembler.Candidate`. **Context:** The SQL predicate is the control; `_row_to_arm_hit` and `_row_to_candidate` assert it held for rows THIS module produced. Every one of those guarantees is a property of `stores/pg/search.py`. An end-to-end test driving the real pipeline over a `CandidateStorePort` fake showed a quarantined row rendering into a context block with nothing raising — because the fake, like any future alternative retrieval driver (PLAN.md §9's Qdrant driver, a second arm, a cache in front of the arms), bypasses that module entirely while still reaching `render()`. **Alternatives:** skipping the offending row instead of raising (rejected — it makes a breached control look like a thin result set, which is precisely the silence this check exists to break); trusting the store (rejected — that is the assumption the test falsified). **Rationale:** One exported function, three call sites, one statement of the rule, and the third call site is the only one an alternative store implementation cannot route around. Failing closed costs one retrieval (the ladder's store-error rung — invariant 2 holds, the run does not fail); failing open costs the entire quarantine guarantee. **Date 2026-07-26.**

## D-071 `retrieval.total_budget_ms` and `embed_timeout_ms` are constrained `ge=1`
**Decision:** Both fields carry `Field(..., ge=1)` in `domain/config.py`. **Context:** `hotpath.budget.Deadline` deliberately refuses a non-positive total budget (a zero budget is a broken config row, not an instant timeout), and `Pipeline` catches that refusal as the ladder's store-error rung — so a `project_config` override of `retrieval.total_budget_ms: 0` was reported to an operator as "the store failed", on every single request, forever. **Alternatives:** treating 0 as an instant timeout (rejected — it silently swallows a broken config row and serves prefix-only in perpetuity). **Rationale:** Rejecting the override where it is parsed names the misconfiguration: `ConfigResolver` raises `ConfigError` quoting the offending section. Deliberately NOT applied to `fused_top_n`/`arm_top_n`, whose non-positive handling is a tested clamp rather than an error. **Date 2026-07-26.**

## D-072 `hotpath.assembler` dedups on `memory_id` as well as `dedup_key`
**Decision:** `_dedup` collapses by `memory_id` first, then by `dedup_key`. **Context:** The function's docstring claimed "the same memory proposed twice" collapses, but it keyed on `dedup_key` alone, so one memory proposed under two different keys survived twice. **Alternatives:** leaving it and correcting the docstring (rejected — two entries for one `memory_id` also means two `injection_log` rows for a table whose primary key is `(project_id, run_id, memory_id)`, i.e. the second row is silently dropped by `ON CONFLICT DO NOTHING` and the audit record disagrees with the prompt). **Rationale:** The two keys catch different duplicates and neither subsumes the other: `dedup_key` identifies content, `memory_id` identifies the row. Collapsing by row first also means the content groups are chosen from already-unique rows, so the winner cannot depend on arrival order. **Date 2026-07-26.**

## D-073 An unusable embedding provider degrades to lexical, exactly like a timeout
**Decision:** `hotpath.retriever` catches `EmbeddingProviderError` alongside `EmbeddingTimeout` and reports `degraded=True`. **Context:** The embedding chunk hardened the Gemini driver so a refused connection, a malformed payload or an over-sized response body raises `EmbeddingProviderError` rather than escaping as a raw `httpx` exception — but the retriever caught only `EmbeddingTimeout`, so every one of those dropped the whole retrieval to the ladder's third rung (`store_error`, nothing at all) through `Pipeline`'s blanket guard, discarding a perfectly healthy lexical arm. **Alternatives:** raising `EmbeddingTimeout` from the driver for provider errors (rejected — it destroys the port's stated contract that a caller can tell "ran out of time" from "the endpoint is broken" without reading exception text). **Rationale:** PLAN.md §2's first rung is "query embedding is unusable -> lexical-only". A broken endpoint leaves the vector arm exactly as unusable as a timeout does, and mislabelling it `store_error` puts "the embedding endpoint is down" and "the memory store is down" in the same bucket on the one row (`retrieval_event`) that exists to tell them apart. **Date 2026-07-26.**

## D-074 `/v1/retrieve` runs a real `Pipeline` when the deployment has one
**Decision:** `AppDeps` gains `pipeline: PipelinePort | None = None` (last, defaulted); `api/main.py::run()` builds a real `Pipeline` — `ConfigResolver` over `Repo`, `Retriever` over `SearchStore` + the configured `EmbeddingPort`, `CandidateAssembly`, `Telemetry` for both `retrieval_event` and `injection_log`, and the killswitch salt read once at startup. `routes_v1.retrieve` uses it when present and otherwise answers exactly as Phase 0 did. **Context:** Nothing in the codebase constructed a `Pipeline`, so the entire hot read plane was unreachable from HTTP; the route's `getattr(deps, "pipeline", None)` was an honest placeholder for a field that did not exist. **Alternatives:** making the field required (rejected — every offline `TestClient` app in the suite, and `create_app`'s "runs with zero services" contract, would need a real pool); building the pipeline inside `create_app` (rejected — `create_app` is pure wiring and must never open a socket or a pool). **Rationale:** `run()` is the one function permitted to build real adapters. `read_salt` raising when the salt is unset is deliberately uncaught: an unsalted arm assignment is *predictable*, so the lift measurement the kill switch reads is compromised from the first request — failing to start is recoverable in a way that silently mis-measuring for two weeks is not. The missing-API-key case fails at startup for the same reason: an embedder that can never succeed makes every retrieval a silent `degraded_lexical`, which looks like a healthy deployment on every dashboard. **Date 2026-07-26.**

## D-075 derived-state audit: the slow reference is the far end of the retained history, not a 30-day mean; a guardrail-flagged reading contributes nothing; `DerivedConfig` is refused where it is consumed
**Decision:** Six changes to the `derived-state` chunk (`workers/baselines.py`, `workers/derived_state.py`), each because the previous behaviour was wrong rather than merely unspecified.
(a) **The slow reference is the earliest reading still inside `SLOW_WINDOW`, not the mean of that window.** The audited implementation computed both references as per-sample means, which measures the drift RATE and not the cumulative displacement the config names. Measured, one reading per day: a 9 %/day walk alarmed on day 6, a 2 %/day walk on day 24, and a **1 %/day walk never alarmed at all** — a trailing mean is computed over the attacked series, lags a geometric walk by roughly half the window, and so has an emergent floor of ~1.6 %/day (≈60 % per 30 days) that nobody chose, against a knob that says 25 %. Anchored on the earliest retained reading the same three walks alarm on days 3, 12 and 23, and the undetectable steady-state drift becomes exactly `divergence_alarm_pct` per `SLOW_WINDOW` (~0.74 %/day). The residual floor is intrinsic and is now pinned by a test (`test_the_detection_floor_is_pinned_not_assumed`) instead of being discovered from an incident. The anchor is also unmoveable by volume: a per-sample mean lets whoever schedules the writer pick the detector's sensitivity (100 readings in one day outvote 29 days of history), and update frequency is a scheduling detail, never a security parameter.
(b) **The alarm compares the slow reference against the value just written as well as against the fast (24h) reference, and reports the larger.** The fast reference is a mean, so three clamp-legal +10 % steps inside one window park the stored baseline 33 % above the reference while the window's own mean is only 21 % above it — and `derived_state`'s consumers read the latest row's `value`, not a mean. With at most one update per fast window the two comparisons are identical, so this adds no false positives for a normally-scheduled writer.
(c) **`DerivedStateUpdateResult.divergence_evaluated` distinguishes "the watchdog found nothing" from "the watchdog could not answer".** A key whose whole history is one instant has nothing to compare against; reporting that as a silent `divergence_alarm=None` is how a blind watchdog looks like a healthy one.
(d) **Both watchdogs are seeded from the store on the first touch of a key** (`DerivedStateStorePort.recent_versions` replaces `latest_version`; one read serves the rate bound, the reference history and the clamp streak, so two reads of the same rows cannot disagree). The reading history and the clamp streak previously lived only in process memory: a restart made the slow reference the first reading written *after* it — i.e. wherever the baseline had already been walked to, forgiving every unit of accumulated drift — and restarted the clamp streak at zero. A deploy did that for free. Measured against the unfixed code, an adaptive attacker writing 12 times a day walked the baseline **2.85x inside the first 24 hours** and was then permanently ahead.
(e) **`guardrail_flagged` (was `excluded_from_reference`) drops the reading entirely.** D-022 says guardrail-flagged runs are "excluded from baseline contribution"; the audited implementation still applied the value to the baseline and only hid it from the divergence windows — the exact inverse, a suspect run moving the stored baseline while being invisible to the one watchdog that could have noticed. No version row is written, the reference history does not see it, the clamp streak does not move.
(f) **`DerivedStateWriter.__init__` refuses a degenerate `DerivedConfig`.** `derived` is an `OVERRIDABLE_SECTION` and none of its four fields carries a pydantic constraint, so all of these arrive from a `project_config` row. `keep_versions: 0` is the dangerous one: it prunes away the row the rate bound reads, so every update looks like a first write and **nothing is ever clamped** — the movement clamp silently removed by a retention knob, with every dashboard still showing a clamp configured. Also refused: a non-positive `baseline_max_delta_pct` (freezes or inverts the bound), `clamp_alert_consecutive < 1` (alerts on every clamp), a negative `divergence_alarm_pct` (alarms always).
**Also fixed:** windows are half-open `(now - window, now]` (closed at both ends, one reading per day put 31 readings in a "30 day" window and two days in a "24 hour" one); a clock that moves backwards is refused, since every window here is measured as "how long ago" and the reference is chosen by age; the rate bound reads the highest-version row rather than the last element of the store's sequence; and the `_clamp_alert_active` latch is now covered by a test that first drives a streak far enough to set it (the existing reset test stopped at two clamps, so a latch that was never cleared survived it).
**Alternatives considered:** weighting each 24-hour period equally instead of anchoring on one reading (implemented and measured — it fixes volume-dragging but not the warm-up hole, and lets the far end of the window be reshaped by whatever landed in its last period; the adaptive attacker still reached 2,935 vs the anchor's 156 over 40 days); adding `derived.slow_window_days` / `fast_window_hours` / `max_updates_per_period` config fields (rejected — hard rule 4 makes an absent field a contract_gap, not a licence to invent one; `SLOW_WINDOW`/`FAST_WINDOW` stay `Final` module constants of this control); putting the config constraints in `domain/config.py` where D-071 put the retrieval ones (rejected — outside this chunk's file list; refusing where the value is consumed is the fix available here and is correct regardless).
**Rationale:** every change above is covered by a test that was observed going red under the exact mutation it exists to prevent — sixteen mutations run (slow reference reverted to a 30-day mean, the value comparison removed, each window boundary closed, the clamp comparison flipped, each of the three config thresholds hardcoded to its default, the guardrail drop removed, seeding removed, the alert latch left uncleared, the backwards-clock guard removed, the `keep_versions` refusal removed, `delta_pct` zeroed, the non-finite refusal removed, `divergence_evaluated` pinned True), zero survivors. **Date 2026-07-26.**

## D-076 The Phase 2 kill switch acts on every injection path, not only on JIT
**Decision:** `hotpath.assembly.killswitched(mem_type, cfg)` is the one definition of "this memory type is disabled", exported and used by both `CandidateAssembly.run` (every `/v1/retrieve`) and `hotpath.jit.JitGate`. A killswitched row is skipped before the abstention gate and is not reported as an abstention.
**Context:** integration grep showed `killswitch_overlay` was read by exactly one module in the whole tree, `hotpath/jit.py`. The ordinary retrieval path never consulted it, so a memory type disabled by PLAN.md section 2's "auto-disable on sustained negative lift" invariant was still retrieved and injected by every ordinary call and disabled only on the side channel.
**Alternatives:** filtering in `stores/pg/search.py`'s SQL (rejected -- the overlay is per-(project, agent_type) resolved config, not a column, and pushing it into the arms would make a config read a query parameter on the hot path); reporting the skip as an abstention (rejected -- `abstained_threshold`/`abstained_rarity` are statements about this query's evidence, and an operator reading the Abstention dashboard would tune thresholds against a row that was never eligible); leaving `jit.py`'s inline `cfg.killswitch_overlay.get(...)` in place (rejected -- two independent readings of one overlay drift, and the drift is invisible because one path keeps injecting while the dashboard says disabled).
**Rationale:** a kill switch honoured on one of two injection paths is worse than none: it moves injection volume onto the unmeasured path rather than stopping it, which also contaminates D-027's stratified lift. The shared function is asserted to be the same object from both modules by test, so the two paths cannot diverge silently. Both still report `empty_result` when everything was disabled -- `OutcomeCode` has no "feature disabled" member, recorded as an open contract gap rather than papered over with a nearby code. **Date 2026-07-26.**

## D-077 A Tier A lane coordinator owns the per-run candidate cap and batch idempotency
**Decision:** `workers/tier_a_lane.py` (`TierALane`) is the single caller of the four Tier A extractors. It constructs exactly one `CandidateCapTracker` per batch and threads it through all four in a fixed order, and it interposes a deduplicating `MemoryWriterPort` that hashes rendered content with `domain.canonical.content_hash` and returns the existing row's id instead of inserting when the project already holds that content. `TierALaneResult.dedupe_is_durable` reports whether the cross-batch half of that guarantee is actually available.
**Context:** the extractors chunk made the shared tracker POSSIBLE (an optional `cap_tracker` parameter) but nothing constructed one, so `tier_a.candidate_cap_per_run = 1` behaved as a cap of 4 in any real deployment. Separately, `Repo.insert_memory_item` is a plain INSERT against a `memory_item` table whose only constraint is `PRIMARY KEY (project_id, id)`, while `queue.lease_seconds`/`max_attempts` make work-item redelivery ordinary -- so a redelivered extraction batch re-inserted every candidate, straight into the metric Phase 2's soak gate measures.
**Alternatives:** a unique index on `(project_id, content_hash)` (correct, and still the structural fix -- rejected here because it is a migration against Phase 0's frozen DDL and there is no Postgres on this machine to verify it against, so it would ship unexecuted); filtering duplicates out of `outcomes` after extraction (rejected -- `emit_candidate` reserves the cap slot BEFORE it renders and scans, so a post-filter lets an already-stored note spend a run's one candidate slot and starve a genuinely new sibling); raising from the writer on a duplicate (rejected -- `emit_candidate` treats any writer exception as fatal to the batch, so one known note would destroy every new note behind it).
**Rationale:** both defects are properties of the lane rather than of any parser, which is why four correct per-extractor suites could not see either. Extractor order is fixed and asserted because the cap is first-come: a lane whose order varied would charge a different note against the same run's one slot from machine to machine, and the soak's vault-growth curve would stop being reproducible. Ten mutations, including restoring the per-extractor tracker, were each applied and each turned the suite red. **Date 2026-07-26.**

## D-078 `NoveltyGate.decide` takes a `project_id` and refuses a foreign-project signature
**Decision:** `ExistingSignature` carries `project_id`, `NoveltyGate.decide` takes one, and any candidate whose project differs raises `TracebedError` -- refusing the whole call rather than skipping the row.
**Context:** `structural_signature` hashes (error_class, tool_id, tool_version, payload_class_hash) and nothing in it names a project, so two projects running the same tool produce byte-equal signatures. The gate was a pure function over signatures with no project anywhere in its types, so a store query that lost its scope would be told to merge, and `merge_provenance` would graft one project's trace_ids onto the other project's memory -- invariant 4 broken through a function that could never have refused. The identity fields cannot supply the missing scope: they are identical by construction in exactly the case that matters.
**Alternatives:** salting the signature with `project_id` (rejected -- it hides the breach instead of naming it, and it would change every signature already computed, including the soak's); skipping foreign rows (rejected -- invariant 4 is not a filter; a store that returned another project's row is a control that has stopped holding, and dropping the row leaves the caller writing to a vault it has already had wrong answers about).
**Rationale:** this seam was invisible to the novelty chunk's own audit because that chunk has no store and its caller did not exist yet. The check has to exist before the caller does, or the caller inherits the hole. **Date 2026-07-26.**

## D-079 A current-version pointer key makes the static prefix cache readable
**Decision:** `stores.valkey.keys.current_prefix_version_key(project_id, agent_type_id)` -- `tb:{project_id}:pxcur:{agent_type_id}` -- holds the live `prefix_version`, with `ValkeyClient.current_prefix_version_get/_set` around it. `PrefixBuilder.run` publishes the block first and repoints the pointer second.
**Context:** `prefix_version` is derived from the packed content, and `hotpath.pipeline.StaticPrefixPort.get(scope)` receives only (project_id, agent_type_id, principal_id); a content-derived version is by construction not computable by a reader that does not already hold the content, and `ValkeyClient` exposes no pattern scan. The prefix builder was therefore correct, deterministic, and completely unreachable.
**Alternatives:** a `prefix_version` column on a registry row (rejected -- a Postgres round trip on the degradation ladder's prefix-only rung, which exists precisely because Postgres is slow or down); a version that is a pure function of (project_id, agent_type_id) (rejected -- it destroys the "identical input set yields an identical version" property the whole cache rests on, since the key would no longer change when the content did); widening the `px:` value to hold both pointer and block (rejected -- the versioned key stays immutable and content-addressed, so two readers resolving the same version can never disagree about the bytes, and a rebuild leaves the previous block readable for requests already in flight).
**Rationale:** publish order is the safety property. A pointer written first names bytes that are not in the cache yet, so every reader in that interval takes a hard miss on the one rung that exists to avoid one; block-then-pointer means the worst intermediate state is a pointer still naming the previous version, stale but complete. A pointer whose stored bytes are not a plain non-negative decimal reads as a miss rather than raising, because `int()` would accept `" 7"`, `"+7"` and `"1_0"` as versions no writer ever published. The key shares the `tb:{project_id}:` prefix, so `delete_project`, `cache_flush` and leak probe 6 cover it with no change. **Date 2026-07-26.**

## D-080 Degenerate `derived`/`lifecycle`/`scoring`/`queue` overrides die at the config layer, and `q_start` must sit above `archive_floor`
**Decision:** `DerivedConfig`, `LifecycleConfig`, `ScoringConfig` and `QueueConfig` carry pydantic field constraints, and `EffectiveConfig` gains a cross-section validator refusing `scoring.q_start <= lifecycle.archive_floor`. `ConfigResolver.effective` translates the resulting `ValidationError` into `ConfigError` so one exception type still means "this project's config rows do not make sense". `DerivedStateWriter` keeps its own refusal as defence in depth, tested against a `model_construct`-ed config the field constraints never saw.
**Context:** all four sections are in `OVERRIDABLE_SECTIONS`, so every degenerate value is reachable from a `project_config` jsonb row, and each one silently DISABLES a control rather than failing: `derived.keep_versions = 0` prunes away the row the movement clamp reads, so every update looks like a first write and nothing is ever clamped. The cross-section case is worse because no single-field constraint can see it -- `workers.sweeps._decayed_q_value` seeds the idle-decay curve at `q_start` and archives at `archive_floor`, so a seed at or below the floor archives every idle `validated` memory on its first idle sweep, from two individually plausible override rows.
**Alternatives:** clamping instead of refusing (rejected -- the sweep already clamps into [0, 1], and a clamp cannot distinguish "decayed to the floor" from "started there"); leaving the refusal only at the consumer (rejected -- that names the misconfiguration one layer too late, and D-071 already set the precedent that the override dies where it is parsed so the resolver can quote the offending section).
**Rationale:** `tier_a.candidate_cap_per_run` is deliberately `ge=0`, not `ge=1`: zero is a meaningful setting (observe and merge, never emit) while a negative cap would reject every run with no way for an operator to tell that from "the extractor found nothing". One existing state-machine test asserted `archive_floor=0.5` against the default `q_start=0.5`; it now uses 0.4, which proves the same projection plumbing against a config that can actually exist. **Date 2026-07-26.**

## D-081 Three seam properties are asserted statically, because no per-module suite can hold them
**Decision:** `tests/phase2/test_write_path_seams.py` asserts, by AST walk over `src/`: every `state_machine.apply()` in `workers/` passes the row's own status and never a `Status.X` literal; no module under `src/` outside `domain/clock.py` calls `datetime.now`/`utcnow`/`date.today`/`time.time`/`time.monotonic*`/`time.perf_counter*`; and both public `insert_memory_item` doors call `validate_provenance`, `content_hash` and `verify_verdict` in that order, with no third caller of the unchecked `_impl_insert_memory_item`.
**Context:** two of the three were real defects found during this phase -- a worker judging a transition against a hardcoded `current` (which makes `apply` authorise an edge PLAN.md section 5's table does not contain, because the guard runs on the evidence while the row is on a different edge) at seven call sites across three workers, and wall-clock reads that would make the 30-simulated-day soak measure intervals the test never set up. Both are mechanical to fix and therefore mechanical to regress.
**Alternatives:** behavioural tests per call site (rejected -- they prove the sites that exist today are right and say nothing about the next one, which is the whole job of a seam guard); a grep-based check (rejected -- several docstrings legitimately contain `datetime.now()` while explaining this very rule, and `from datetime import datetime as dt` would hide a real violation from any regex).
**Rationale:** each assertion was verified to fail under the exact defect it exists to catch, by applying that defect to the tree and reverting it. The wall-clock check carries a guard-the-guard companion asserting that `domain/clock.py` still reads a wall clock, so the exclusion cannot go vacuous. **Date 2026-07-26.**

## D-082 The obvious SQL fix for lease-stealing `nack()` is unsound and was NOT applied
**Decision:** `_NACK_SQL` stays `UPDATE work_queue SET available_at = now() + %(backoff)s, lease_expires_at = NULL WHERE id = %(id)s`. The lease-overrun defence stays where the runner chunk put it: client-side in `WorkerRunner`, which declines to nack a batch whose own claim lease has elapsed and counts `WORKER_LEASE_OVERRUNS`.
**Context:** `nack()` clears `lease_expires_at` with no ownership check, so a consumer that outran its lease can hand a row away from whichever consumer has since claimed it. The natural store-side fix is to add `AND lease_expires_at > now()` to the WHERE clause.
**Alternatives:** that clause (rejected -- it is backwards. If the lease merely expired and nobody reclaimed the row, `lease_expires_at` is in the past and the guarded nack becomes a no-op, losing the backoff for the SAFE case. If another consumer has since reclaimed the row, `lease_expires_at` is that consumer's FUTURE timestamp, so the predicate is true and the theft proceeds. The guard refuses exactly the harmless case and permits the harmful one); a lease-holder token column (correct, and the real fix -- a migration against Phase 0's frozen DDL plus a `renew` primitive on `QueueConsumerPort`, neither verifiable without a Postgres).
**Rationale:** recorded because the wrong fix is more obvious than the right one, and shipping SQL that cannot be executed on this machine would have made a correctness claim nothing checked. Until a lease token exists, `attempts`/`max_attempts` still bound the retry budget, so the residual cost of an unguarded overrun is a lost backoff interval and possible duplicate processing -- which is why every consumer behind `QueueConsumerPort` is required to be idempotent in the first place. **Date 2026-07-26.**

## D-083 `scoring.updates_per_memory_per_day` gains a floor of 1 and deliberately no ceiling
**Decision:** `ScoringConfig.updates_per_memory_per_day` becomes `Field(default=1, ge=1)`. No upper bound is added, and `tests/phase3/test_learning_loop_seams.py::test_the_daily_scoring_cap_still_has_no_upper_bound` pins that absence so whoever closes it must record the number they chose.
**Context:** `scoring` is an `OVERRIDABLE_SECTIONS` member, so a `project_config` jsonb row reaches this field, and it was the one scoring-adjacent value in the module with no constraint at all (`alpha` is `gt=0, le=1`, `q_start` is `ge=0, le=1`, and D-080 constrained the rest of the section). Unlike `promotion`/`retirement`/`lifecycle` it is NOT projected into `TransitionLimits`, so the invariant floors added there do not cover it and nothing downstream floors it either — `run_scorer_batch` honours whatever it is given. Verified by construction: `ScoringConfig(updates_per_memory_per_day=1000)` and `=-5` were both accepted before this change.
**Alternatives:** `le=1` (rejected — it makes the field pointless, and PLAN.md §6 lists it in the tunable config table rather than as a fixed count like `SHADOW_CONFIRM_MIN_INDEPENDENT`); some invented ceiling such as `le=10` (rejected — hard rule 4 forbids magic numbers, and a ceiling on a governed threshold is exactly the kind of number that must not be guessed; sibling chunks made the same call for `MAX_DIFF_CHARS` and recorded it rather than retuning it); leaving the field unconstrained (rejected — `0` and negatives make `scored_today >= cap` true on every call, which does not narrow the cap but turns scoring OFF for the entire project, silently, with every worker still running and every gate still green).
**Rationale:** the two directions fail differently and only one of them can be fixed without inventing policy. Downward is a silent total disablement and the floor is stated by PLAN.md §6's own default, so it is closable now. Upward is the genuinely dangerous direction — the daily cap is what bounds how fast a single feedback source can walk a memory's Q, and D-021 sizes its four-calendar-day retirement window on this being 1 — but PLAN.md §6 gives the field a default and no range, so the ceiling is a human's call, not an integrator's. Recorded rather than guessed. **Date 2026-07-26.**

## D-084 Forensic containment of a poisoned `validated` memory is reversible, and that is recorded rather than fixed
**Decision:** `workers.forensics` keeps containing a poisoned `validated` row to `stale` (the nearest legal edge), no `validated -> quarantined` edge is added to `TRANSITIONS`, and the reversibility is pinned as data (`BlastRadiusReport.containment_reversible_by`) plus a test class (`TestContainmentIsReversible`) that asserts the current, defective behaviour on purpose.
**Context:** PLAN.md §8 improvement 1 describes the Recall & Rollback flow as "quarantine a memory". PLAN.md §5's table — which that same document calls "the test source" — contains no `validated -> quarantined` edge and lists everything absent from it as illegal. The two statements contradict each other. Meanwhile `stale -> validated` requires `evidence.reverified` and nothing else: no independence recheck, no scan re-pass, no awareness that a poisoning finding exists. `revalidation.is_due_for_revalidation` makes a `stale` row due on any LATER instant, so the next scheduler tick suffices. Driven end to end through the real state machine at integration: containment lands on `stale`, the row is due one simulated second later, and a verifier returning "still correct" returns it to `validated` and to `RETRIEVABLE_STATUSES`. An OEP-shaped locally-correct poisoned memory is precisely the kind a verifier re-verifies successfully, so this is the expected case, not the unlucky one.
**Alternatives:** adding `(VALIDATED, QUARANTINED)` to `TRANSITIONS` (rejected — PLAN.md §5's table is the authoritative contract and explicitly closes everything it does not list; an integrator adding a governance edge to it unilaterally is the "admin bypass with extra steps" the table's own read-only comment warns about); containing to `superseded` (rejected — requires asserting a contradiction that does not exist, and has no recovery edge at all); containing to `archived` (rejected — requires fabricating `decay_floor_reached`, and see D-085: `archived` is not durable either); driving the row to `retired` by writing `strike_count=2` (rejected — the second strike names a second OCCASION, and manufacturing one from a single finding is exactly the evidence fabrication every guard in this module refuses); making `revalidation` skip rows with an open `review_queue` item (the correct fix, and blocked — `review_queue` has no read-back method anywhere in the tree and no dedup key, per the forensics and promotion chunks' own reported gaps).
**Rationale:** every available in-contract action either fabricates guard evidence or silently changes the governance contract, and both are worse than a recorded hole. What is fixable now is visibility: the reversibility is machine-detectable on the report rather than living only as English prose in a `review_queue.reason` column nothing reads, and the test class fails the moment someone closes the hole, forcing this entry to be updated rather than quietly outdated. The one durable containment that exists today is `tombstoned`, which is asserted. **Date 2026-07-26.**

## D-085 `archived -> validated` re-checks D-023; the broader restore gap is recorded
**Decision:** `_guard_archived_to_validated` now refuses `ProvenanceClass.PROPOSAL` before it considers `operator_restore`. The wider case — an operator restoring any Tier B row that was quarantined, TTL-expired uncorroborated, and never earned `validated` in the first place — is recorded here and pinned by `test_operator_restore_is_the_only_route_to_validated_that_skips_independence`, not closed.
**Context:** found at Phase 3 integration by exhaustive reachability search over `TRANSITIONS`, not by the red team. D-023 ("proposal class: no skip ever applies") was enforced in exactly one guard, `_guard_quarantined_to_candidate`. But `archived` is reachable from `quarantined` on plain TTL expiry, and `archived -> validated` checked `operator_restore` and nothing else. The complete path was three legal edges — `None -> quarantined` (propose_memory, Tier B, scan passes), `quarantined -> archived` (30-day TTL, zero corroboration), `archived -> validated` (operator restore) — landing proposal-class content in `RETRIEVABLE_STATUSES` having never invoked `independent_confirmations` and never faced the promotion predicate. Verified by direct drive before fixing. This matters most for how the Phase 3 red team reads: all four adversarial probes terminate in `archived` and were recorded as "furthest_status: archived, never validated", which reads as containment and was in fact one unguarded edge from full retrievability. `archived` is not a terminal status and no report may treat it as one.
**Alternatives:** forbidding `archived -> validated` outright (rejected — PLAN.md §5 row 15 permits it explicitly as "operator restore (recoverable, logged)", and removing it would destroy the only recovery path for a legitimately decayed memory); routing restore through `candidate` instead of `validated` so the promotion predicate applies (rejected as an integrator action — it changes an edge PLAN.md §5 states, and `candidate` is Tier-A-only on the retrieval side, so a restored Tier B row would land somewhere the hot path treats differently); adding a "was ever validated" field to `TransitionEvidence` so the guard can distinguish the intended restore from the never-corroborated one (the correct general fix, and out of reach — that dataclass is frozen Phase-0 contract).
**Rationale:** the PROPOSAL case needed no new policy: D-023's wording is "no skip EVER applies", and a restore that reaches `validated` without passing through `candidate` is the largest skip in the table, so re-checking it here restores a stated invariant rather than inventing one. The broader case genuinely needs a human, because the guard cannot see the history that distinguishes the permitted restore from the impermissible one, and the field that would let it see is frozen. The reachability search is kept as a test so a third route to `validated` cannot appear unnoticed. **Date 2026-07-26.**

## D-086 The Phase 3 red team's "never reaches validated" is conditioned, and the gate now says so
**Decision:** `harness/phase3_gate.py`'s `_KNOWN_GAPS` gains a leading entry stating that `archived` is not terminal, that all four adversarial probes terminate there, and that clause 2's PASS therefore means "no probe reached `validated` along the corroboration path it attacked", not "no probe can reach `validated`".
**Context:** the four probes are refused by the D-020 independence computation and then swept to `archived` by the real quarantine TTL. The gate recorded `furthest_status: archived` for all four and asserted `reached_validated == False`, which is true of what was executed. It is not the property the clause's wording implies, because `TRANSITIONS[(ARCHIVED, VALIDATED)]` exists and the harness never exercises it (see D-085).
**Alternatives:** extending the probes to attempt an operator restore (rejected for this pass — `operator_restore` is a deliberate human action, so a probe that asserts it is modelling a malicious or mistaken operator, which is a different threat model than the four PLAN.md §7 names, and inventing it inside a gate is how a red team starts grading itself on scenarios it chose to be able to pass); silently leaving the wording (rejected — a gate that claims more than it executed is the failure mode this whole audit pass exists to catch, and this clause is the single most load-bearing claim in the phase).
**Rationale:** a red team written by the same fleet that wrote the defences is the most self-serving artifact in the repository, and the specific way that bias shows up is not fabricated passes but unstated conditioning — every probe stopping at the same status, that status being treated as terminal because nothing asked whether it was. The finding came from an exhaustive reachability search over the transition table, which is adversarial in a way a hand-written probe list structurally cannot be, because it does not know which scenarios are flattering. **Date 2026-07-26.**

## D-087 The proposal caps become a durable, cross-process control; the in-process lock is demoted to a pre-check
**Decision:** `stores.pg.repo.Repo` gains `insert_proposal_within_caps`, which runs the redelivery check, the per-run cap, the per-project-per-UTC-day cap and the `memory_item` INSERT inside ONE transaction holding a project-scoped `pg_advisory_xact_lock(PROPOSAL_CAP_LOCK_CLASS, hashtext(project_id::text))`. `workflow.agent_control.AgentControl` calls it whenever the injected store satisfies the new optional `DurableProposalCapPort`, and that result — not the in-process count — is authoritative. The three read-only queries `AgentControlRepoPort` declared (`count_proposals_in_run`, `count_proposals_in_project_day`, `find_proposal_in_run`) are implemented on `Repo` at the same time, so the port is satisfied by the shipped store rather than by fixtures only. `AgentControl.durable_caps` reports which guarantee is in force, and `workers.runner.run()` refuses to start a proposal consumer wired to a store that cannot give the durable one.
**Context:** Phase 4 landed the caps behind `AgentControl._cap_lock`, a `threading.Lock`. That makes them exact for every submission ONE process handles and was correctly recorded by the builder as a contract gap: two API/worker processes sharing one Postgres each observe `count == cap - 1` and each insert, so the real bound was `cap x concurrent processes`. `proposals.per_run_cap` / `per_project_daily_cap` are the only control that stops an agent flooding its own vault with self-authored beliefs (PLAN.md §6), so a control that holds only in the single-consumer deployment is a control that stops holding exactly when the fleet grows.
**Alternatives:** (a) `INSERT ... SELECT ... WHERE (SELECT count(*) ...) < cap` — one statement, no lock, but the sub-select and the insert are not mutually exclusive under READ COMMITTED, so it has the identical race in SQL rather than in Python. (b) A partial unique index — enforces a fixed cap, not a configurable one, and `per_run_cap` is an `EffectiveConfig` field a project can override. (c) `SELECT ... FOR UPDATE` on the existing proposals — there is no row to lock when the count is zero, which is precisely the case that must be serialised. (d) `SERIALIZABLE` for this transaction — would work, but turns a cap refusal into a serialisation failure the caller must retry, on a path that already has an at-least-once queue behind it.
**Rationale:** An advisory lock is the one mechanism that serialises a COUNT. It is transaction-scoped, so it is released by COMMIT or ROLLBACK with no unlock path to forget, and contention is bounded by the very control it protects (50 proposals/project/day). The in-process pre-check is kept, not deleted: it refuses an over-cap proposal before it pays for the scan suite, and it is explicitly documented as an optimisation that never decides the outcome. The day bounds are computed in Python as an explicit half-open UTC range rather than `DATE(created_at)`, because `DATE()` on a `timestamptz` renders in the session's `TimeZone` and would have made a per-UTC-day cap silently become a per-server-local-day cap.
**Date 2026-07-26.**

## D-088 `blackboard_entry` carries its own guarantees: `author_agent uuid`, `value_ref`/`status` NOT NULL
**Decision:** `migrations/0002_partitioned.sql` types `blackboard_entry.author_agent` as `uuid NOT NULL` (was `text NOT NULL`) and declares `value_ref` and `status` `NOT NULL` (both were nullable). `stores.pg.blackboard.BlackboardRepo.commit` binds the `PrincipalId` itself instead of `str(...)`. `_row_to_entry`'s NULL check is kept and re-documented as defence in depth rather than the primary control.
**Context:** `BlackboardEntryRow` types `value_ref` and `status` as `str`, and `resolve_after_conflict` compares `value_ref`s to decide "converged" versus "conflict". A NULL reaching that comparison reported a conflict won by a value that does not exist. `author_agent` is always a `PrincipalId` — server-derived, never caller-asserted — and `_row_to_entry` constructs one from the column, so a non-UUID row from any other writer made every read of that entry raise. `harness/leak_suite/fixtures.py` seeded a row violating all three.
**Alternatives:** (a) Leave the schema and keep compensating in the repository — rejected: every reader then re-checks what one constraint could state once, and the compensation is invisible to anything that queries the table directly. (b) Widen `BlackboardEntryRow.author_agent` to `str` — rejected: that drops the domain newtype that makes `author_agent` non-interchangeable everywhere else. (c) Add a new `0004_*.sql` ALTER migration — rejected because no environment has ever applied these migrations (there is no Postgres in this build) and splitting one table's declaration across two files makes the declaration harder to read than the change is risky.
**Rationale:** The schema is where a guarantee about the shape of a row belongs. Note the consequence deliberately taken on: with `author_agent` typed `uuid`, binding a Python `str` would now be rejected by Postgres outright ("column is of type uuid but expression is of type text"), which is why the parameter binding changed in the same commit — `stores.pg.pool.register_typed_id_adapters` declares the uuid OID for `TypedId`, and that is the convention every other builder in `repo.py` already follows.
**Date 2026-07-26.**

## D-089 `abstention.target_abstention_pct` becomes a real config field, and is documented as reporting-only
**Decision:** `AbstentionConfig` gains `target_abstention_pct: float = Field(default=50.0, ge=0.0, le=100.0)`. `harness.negative_probes.run_negative_probes` reads it off the resolved `EffectiveConfig` instead of a harness-local literal. Nothing in `hotpath/` reads it, and its docstring says so.
**Context:** PLAN.md §6 lists the field with a documented target of ">= 50", and four places quoted the number — `harness/negative_probes/probes.py`, `harness/phase1_gate.py`, `hotpath.abstention.measured_abstention_rate`'s docstring and `workers.lift`'s — while `AbstentionConfig` did not have it. Two of those four described the absence as a contract gap; the harness carried its own copy of the literal.
**Alternatives:** Leave it absent and keep the harness literal — rejected: a documented config path that does not exist is the kind of thing a later reader "fixes" by inventing a second one somewhere else.
**Rationale:** It is a fleet-level REPORTING target, not a threshold any decision reads, and that distinction is the reason it was easy to leave out. An abstention rate is a property of a population of retrievals; the hot path only ever sees one, so a per-call comparison would be meaningless and would put a reporting number on a decision path. Bounded to a real percentage because `abstention` is an `OVERRIDABLE_SECTIONS` member: a `project_config` row reaches this field, and a target of 900 makes the probe gate unpassable while -1 makes it vacuous.
**Date 2026-07-26.**

## D-090 `TOPIC_MEMORY_PROPOSAL` gets a real consumer loop, and it is not a `WorkerRunner` handler
**Decision:** `ProposalIntake` gains `run_forever(stop, *, poll_interval_s, max_iterations=None)`, and `workers.runner.run()` starts it on a third supervised thread beside the ingest loop and `WorkerRunner`, polling at the cadence the ingest loop was given (`ConsumerRunner.config.poll_interval_s`, newly exposed read-only) rather than a fresh invented interval.
**Context:** `api.routes_v1.propose_memory` enqueued onto `TOPIC_MEMORY_PROPOSAL` from Phase 0 and `ProposalIntake` was written in Phase 4, but nothing in any real process ever claimed that topic. The API answered 202, the agent believed its proposal was accepted, and the row sat in `work_queue` until it was swept — the failure being invisible on both sides is what makes it worth a decision entry rather than a commit message.
**Alternatives:** Register `ProposalIntake` as a `workers.runner.BatchHandler`, which is the shape the runner already has. Rejected on a real property: `WorkerRunner` acks or nacks a WHOLE batch on one handler outcome, while this consumer's entire ack/nack policy is per item — a scan rejection and a cap refusal are acked (deterministic on the item's own bytes; retrying only wastes a claim slot on a decision that cannot change), a store error is nacked with backoff. Folding it in would collapse those into one verdict for every item claimed in the same round, turning one refused proposal into a retry storm for the innocent items beside it.
**Rationale:** `ingest.runner.ConsumerRunner` already establishes the shape for a consumer that does its own claiming; this mirrors it, including `max_iterations` (so the loop is finite and a test can drive it to completion) and `stop.wait()` rather than `time.sleep()` (so shutdown during an idle wait is immediate). All three of `stores.pg.queue`'s topics now have a consumer, which is why `WorkerRunner`'s handler map being empty is now an honest statement rather than a gap.
**Date 2026-07-26.**

## D-091 Project-scoped cache flushing is composed, not remembered
**Decision:** `workflow.prefetch` gains `combined_project_flush(*flushes)` and `PrefetchingRetriever.project_flusher()`. A deployment composes every project-scoped cache tier into the single `Callable[[ProjectId], int]` that `workers.invalidator.Invalidator(flush_cache=...)` already accepts. Every tier runs even if an earlier one raises; the first exception is re-raised after all of them have been attempted.
**Context:** PLAN.md §5 says per-project key sets are tracked for O(1) flush and a `cache_flush` invalidation event exists. That is a claim about every cache holding project-scoped data, and Phase 4 added a second tier: `PrefetchingRetriever` caches whole retrieval outcomes in process. A wiring site that passed only `stores.valkey.flush.flush_project_cache` would flush half the caches and report success — the same failure `Invalidator` already refuses to have for the no-flusher case, where it reports `cache_flushed=False` rather than claiming a flush it did not perform.
**Alternatives:** (a) A module-level registry of live caches — rejected: global mutable state, and a cache that forgot to register is invisible. (b) Import `workflow.prefetch` from `stores/valkey/flush.py` — rejected as a layering inversion. (c) Continue-on-error that swallows the exception — rejected: a caller told the flush succeeded when a tier is down keeps serving the invalidated data.
**Rationale:** Composition at the wiring site is the only place that knows which tiers exist. Stopping at the first failure would leave later tiers serving exactly the data the event exists to remove: a partial flush reported as a total failure is recoverable by a retry, a partial flush that silently skipped a tier is not. **The gap this does NOT close is recorded plainly:** nothing in `src/` reads `invalidation_event` rows back off the table — `POST /v1/invalidation` writes them and no loop drains them — so there is still no live consumer to hand the composed callable to, and `max_age_ms` remains the only bound actually in force on a prefetched result outliving an invalidation.
**Date 2026-07-26.**

## D-092 `docker/compose.yaml` set a master-key variable nothing reads
**Decision:** The `api` service sets `TB_MASTER_KEY`, not `TB_CRYPTO__MASTER_KEY`.
**Context:** `crypto.shred.EnvMasterKeyProvider` reads the bare name `TB_MASTER_KEY`, deliberately outside `TracebedSettings` (C-15: settings objects get `repr()`'d into logs). The compose file set a `TB_CRYPTO__`-prefixed name that nothing in the tree reads, so the `full`-profile `api` container aborted at startup with `MasterKeyMissing` even when an operator supplied a key. Verified empirically that an unknown `TB_CRYPTO__*` variable does not raise a settings `ValidationError` under `extra=forbid`, so the defect surfaced only as the confusing `MasterKeyMissing`, never as "you set a variable that does not exist".
**Rationale:** Logged rather than fixed silently because the same mistake was simultaneously present in `docs/OPERATIONS.md` (twice, including in backup guidance that would have had an operator back up an empty secret and lose the only key that can unwrap every subject KEK in every snapshot). One wrong name in two documents and one compose file is a naming hazard worth a record: a secret that is deliberately NOT a settings field has no schema to catch a typo.
**Date 2026-07-26.**

## D-093 The control plane gets seven read-only routes; the kill switch does not get a write one
**Decision:** `api/admin.py` gains `GET /admin/whoami`, `/admin/memory`, `/admin/review_queue`, `/admin/killswitch_state`, `/admin/invalidations`, `/admin/spend` and `/admin/config`, all `ScopeDep`-authenticated exactly like `GET /admin/memory/{id}`. `Repo` gains the four readers they needed and did not have (`list_review_items`, `list_killswitch_state`, `list_invalidation_events`, `spend_since`); the other three routes read methods that already existed (`list_memories`, `get_project_config`, `get_agent_type_config`). They are reached through one new optional `AppDeps.control_plane: ControlPlaneReadPort | None`, which fails CLOSED (`ConfigError` → opaque 500) rather than returning an empty list when a deployment wired no reader. **No write route was added for any of them** — in particular there is no `POST /admin/killswitch_state`, and the dashboard's kill-switch override control was deleted rather than pointed at one.
**Context:** Every dashboard view in the four governance/observability groups was either export-backed (a full-project NDJSON dump with no filter, no window and no pagination) or fixture-backed. Five tables the control plane owns — `review_queue`, `killswitch_state`, `invalidation_event`, `spend_ledger`, `project_config` — were written by workers and routes and read by nothing over HTTP, so views for them had been built against hand-authored constants: a fabricated Q-trajectory line chart, a fabricated status-transition timeline with synthesised timestamps, a fabricated derived-descendant chain whose `.length` was passed into a destructive action's impact list. The dashboard could not even name the project a credential resolved to.
**Alternatives:** (a) Leave the views on `GET /export/project` — rejected: every figure then becomes an all-time total computed client-side from an arbitrarily truncated slice, and the truncation point is not controllable from the client, so a week-over-week figure can sit on an incomplete week. (b) Add the aggregate report routes the views were written against (`/admin/lift/report`, `/admin/staleness/report`, `/admin/consolidation/diffs`) — rejected: those are computations (stratified lift with BH correction, consolidation deltas), no table persists their output, and a route that recomputed them per request would be a second author of a governing number whose first author is `workers/killswitch.py`. Those three views were removed instead. (c) Add a kill-switch write route so the override control could work — rejected: PLAN.md §10 says no admin bypass exists in code, `evidence["source"]` is how an operator override is distinguished from an automatic trigger, and that authorship belongs to `workers.killswitch.apply`, not to an HTTP handler a browser can reach.
**Rationale:** A governance console that cannot read a table cannot govern it, and the observed failure mode of leaving it unreadable was not a blank screen — it was invention. Read-only is the whole shape of the concession: seven `GET`s, each project-scoped by the same `ProjectScope` mechanism the leak suite's probe 3 already covers, none of them accepting a project id in any position, and none of them able to change a status. `GET /admin/whoami` is the one that reports a `project_id` at all, and it reports the OUTPUT of `Repo.resolve_project` — reporting the derived scope is the inverse of accepting one, and a caller sending it back on any other route is still a 422. `?status=` is typed as the `Status` enum so a misspelling is a 422 rather than a filter that matches nothing, because "there are no quarantined items" and "you spelled quarantined wrong" render identically and only one of them means the vault is clean. Limits are bounded twice (wire and repository) for different readers: the repository's ceiling protects the database from any caller, the wire's 422 tells a dashboard author they asked for more than the route will ever return instead of handing back a shorter page they might read as a total.
**Date 2026-07-26.**

## D-094 Four dashboard views are deleted rather than kept on fabricated data
**Decision:** `dashboard/src/views/LiftAndQ.tsx`, `Consolidation.tsx`, `Staleness.tsx` and `Health.tsx`'s worker/queue/xmin sections are removed, along with `Projects.tsx`'s fixture registry tables and every remaining `FIXTURE_*` constant in the tree. `Overview` drops its fixture stratified-lift tile. The nav loses Lift, Consolidation and Staleness; Kill Switch, Review Queue and Spend stay and are re-pointed at the D-093 routes.
**Context:** The task brief's rule is "do NOT leave a view rendering fixture data while pretending to be live". Each removed surface had no data source of any kind: per-item Q history is stored nowhere (`memory_item` carries one current `q_value` and one `status_changed_at`, no transition log); the incremental-delta consolidator persists no diff table; `memory_link` has neither an export table nor a route, so derived-descendant blast radius is not computable; worker heartbeats, `work_queue` depth and the xmin horizon have no per-project table and `work_queue` is unpartitioned, so a route over it would report other projects' backlog.
**Alternatives:** Keep each view with an `EmptyState` naming its gap. Rejected for the three whose entire content was the missing computation — a nav entry that always says "no route feeds this" trains an operator to ignore the nav. Kept for the sections inside surviving views (`Forensics`' descendants panel, `MemoryDetail`'s Q history) where the surrounding view has real content and the gap is a specific, nameable absence beside it.
**Rationale:** A removed view is honest; a view that renders a plausible number nobody measured is worse than no view, and this console's users are deciding whether to trust what the memory system learned. The three deleted views can return the moment something persists what they were drawing: a `q_history` table, a consolidation-diff table, and `memory_link` over HTTP. That list is short and concrete, which is the point of deleting rather than stubbing.
**Date 2026-07-26.**

## D-095 Supersedes D-093 and D-094: the three report routes and three dashboard views SHIPPED
**Decision:** `GET /admin/lift/report`, `GET /admin/staleness/report` and `GET /admin/consolidation/diffs` exist (`api/reports.py`), and `LiftAndQ.tsx`, `Consolidation.tsx` and `Staleness.tsx` exist, are routed (`dashboard/src/App.tsx`) and are in the nav. D-093's alternative (b) and D-094's decision are both REVERSED. This entry is the record; neither earlier entry is edited (append-only).
**Context:** A fidelity audit read DECISIONS.md as the authority and got a system that does not exist: D-093 rejects the three routes verbatim ("a second author of a governing number whose first author is `workers/killswitch.py`") and D-094 states the three views "are removed" and "the nav loses Lift, Consolidation and Staleness". All six were present in the tree, with file mtimes about an hour after D-094 was written, and the reversal was explained only in a TypeScript comment. An audit trail whose last two entries are false about the shipped tree is worse than a missing one: a reviewer stops reading the code.
**Alternatives considered:** (a) Revert the tree to match D-093/D-094 — rejected: the routes and views are the better outcome, and the console is materially more useful with them. (b) Edit D-093/D-094 in place — rejected: the file's own header says never edit an entry, and there is no git history here to make an in-place edit detectable. (c) Leave the reversal in a code comment — rejected: that is the failure being corrected.
**Rationale:** D-093's hazard is real and is accepted with its mitigation named rather than waved away: `api/reports.py` recomputes Benjamini-Hochberg alongside `workers/killswitch.py`, so there are now two implementations of one governing correction. The mitigation is that the route is READ-ONLY and cannot act — only `workers.killswitch.apply` can disable a memory type — so a divergence between the two produces a wrong NUMBER on a dashboard, not a wrong DECISION in the vault. That is a defect to fix by collapsing the two onto one function, and it is recorded as an open item in PLAN.md's known-gaps section rather than as a solved problem.
**Date 2026-07-26.**

## D-096 Invariant 7 covers CREATION, not only transitions
**Decision:** `domain.state_machine.LEGAL_CREATION_STATUSES` (derived from the `(None, X)` edges of the transition table) plus `assert_legal_creation_status`, enforced in BOTH `domain.memory.NewMemoryItem.__post_init__` and `stores.pg.repo.Repo.insert_memory_item` (all three insert paths).
**Context:** The transition table is the strongest suite in the tree, but it only governs `apply()`. `insert_memory_item` bound `item.status.value` straight through and `migrations/0002_partitioned.sql`'s CHECK admits all nine statuses, so a caller could insert a directly-retrievable `validated` row having never called `apply()` — while `NewMemoryItem`'s docstring asserted, twice, that "the repository re-checks that the status is a legal creation status". It did not.
**Alternatives considered:** (a) Enforce in the repository only — rejected: a fake or a future driver bypasses it, and the check belongs where the object is built. (b) Narrow the DB CHECK to the three creation statuses — rejected: the same column must accept all nine on the (not yet built) status-write path, so the constraint would have to be dropped again the moment learning is wired. (c) Write the three statuses out as a literal — rejected: it would drift from the table it is derived from.
**Rationale:** Derived, not written: adding a `(None, X)` edge makes X insertable automatically, and nothing else can. `harness/latency_bench.py` seeded `validated` rows through this path and now seeds Tier-A `candidate` rows (identically retrievable) with the promotion it would rather do recorded as an assertion, so the bench turns red the day a status-write path lands rather than silently continuing to fabricate state.
**Date 2026-07-26.**

## D-097 Retrieval enforces `scope_type`/`scope_id`, fail-closed for the two scopes nothing resolves
**Decision:** `stores.pg.search.CandidateRow` carries `scope_type`/`scope_id` (no default), `fetch_candidates` selects them, and both injection paths (`hotpath.assembly.CandidateAssembly`, `hotpath.jit.JitGate`) drop any row `domain.visibility.scope_visible` refuses. `project_shared` is visible to every run; `agent_type` is visible iff it matches `ProjectScope.agent_type_id`; `workflow_template` and `user` are visible to NOTHING today, because `/v1/retrieve` carries those two as free text and nothing resolves either to a `scope_id`.
**Context:** Every statement in `stores/pg/search.py` filtered on `project_id` and the retrievability predicate and on nothing else, and `CandidateRow` did not even carry the two columns — so a memory written for one end user was retrievable by any agent serving any user in the same project. MEMORY_PLAN §5's ownership model was written, stored, and read by no query.
**Alternatives considered:** (a) Add the predicate to the SQL arms instead — rejected for now: the arms take no agent identity, so every port signature and every test double would change; the assembler already holds `ProjectScope` and is the last hop before rendering. The SQL predicate remains the better long-term control and is recorded as an open item. (b) Resolve `user_ref`/`workflow_template` to ids here — rejected: there is no workflow-template registry and `user_ref` is a subject reference, not a subject-key id; guessing an identity is how a scope check becomes a scope leak. (c) Treat an unresolvable reference as "visible" — rejected outright: that is the current defect with a comment attached.
**Rationale:** Fail-closed narrows what a run can see, which is the safe direction, and it is visible: a preference or user-scoped memory that stops appearing is a missing feature, whereas the previous behaviour was a silent cross-user read. `scope_visible` is exhaustive over `ScopeType` via `assert_never`, so a new scope type is a type error rather than a silent default.
**Date 2026-07-26.**

## D-098 `trace_index.arm` is derived from `retrieval_event`, never accepted from a caller
**Decision:** `stores.pg.rows.TraceIndexUpsert` loses its `arm` field entirely; `Repo`'s trace-index upsert derives the column from a scalar subquery over `retrieval_event.arm` (oldest row for the run, `COALESCE` to `memory_on`) on both the INSERT and the ON CONFLICT branch; `stores.pg.reports.lift_observations` stratifies on `re.arm` rather than `ti.arm`.
**Context:** PLAN.md §10 forbids "accept … arm assignment from any caller" in those words. `ingest.trace_writer` read `arm` out of the caller-supplied `run_start` payload, `Repo` made a declared `holdout` sticky, and `reports.py` fed that caller-controlled column into the governing stratified-lift number — while already joining `retrieval_event`, whose `arm` the server wrote itself.
**Alternatives considered:** (a) Keep the field and validate it — rejected: a validated caller assertion is still a caller assertion, and the sticky-holdout rule made a hostile "holdout" permanent. (b) Leave `trace_index.arm` unwritten and read `retrieval_event` everywhere — rejected: the column is PLAN.md §5's kill-switch audit column and a NULL/default there is indistinguishable from a genuine `memory_on`.
**Rationale:** A shape that cannot carry the value cannot smuggle it, which is why this is a deleted field rather than an ignored one. The subquery costs one indexed lookup per trace-index upsert (primary key `(project_id, run_id)`) on the ingest path, which is asynchronous by invariant 5.
**Date 2026-07-26.**

## D-099 The holdout arm is memory-off, and shadow-retrieves
**Decision:** `hotpath.pipeline.Pipeline.retrieve` runs the full ladder on the holdout arm, records `injection_log` rows as usual, then stamps `OutcomeCode.HOLDOUT` on the `retrieval_event` row and returns an EMPTY context block to the caller.
**Context:** MEMORY_PLAN §5 says 5% of runs execute memory-off; the pipeline computed the arm, logged it, and returned the identical rendered block on both arms. Every lift figure was therefore a comparison of memory-on against memory-on — the earn-your-context loop could not fail because there was no counterfactual. `workers/lift.py` reported this in its own docstring as a defect it could not fix from its module.
**Alternatives considered:** (a) Skip retrieval entirely on the holdout arm — rejected: PLAN.md §7 stratifies lift on "runs where something was actually injected vs SHADOW-RETRIEVED holdout", and `workers.lift.is_shadow_control` uses the presence of an `injection_log` row to decide a holdout run would have placed a memory. No retrieval means an empty control bucket and a kill switch that can never fire. (b) Keep returning the block and mark the row — rejected: that is a labelled placebo, not a control.
**Rationale:** The agent gets nothing; the measurement gets everything. Holdout assignment failing still falls back to `memory_on`, so a broken hash cannot silently withhold memory from every run. Test suites that mint random scopes now pin `killswitch.holdout_pct = 0` explicitly, because at the shipped 5% default roughly one run in twenty would otherwise have drawn an empty block at random — which is a real property of the system and was worth surfacing.
**Date 2026-07-26.**

## D-100 Four governed thresholds move from module constants into typed config
**Decision:** `killswitch.fdr_alpha` (0.05) and `killswitch.confidence_level` (0.95) are added to `KillswitchConfig`; `scoring.contribution_rubric` ({NONE:0, PARTIAL:0.5, FULL:1.0}) and `scoring.contribution_judge_temperature` (0.0) are added to `ScoringConfig`. `workers.lift.DEFAULT_BH_ALPHA`/`DEFAULT_CONFIDENCE` and `workers.contribution_judge._RUBRIC`/`TEMPERATURE` now READ those fields; `tests/phase0/test_config.py` pins each pair.
**Context:** Hard rule 12 says every number comes from typed config. These four were module constants with CONTRACT GAP comments explaining that PLAN.md §6 named no field — which is how a governed threshold becomes a magic number with an excuse attached. `killswitch.correction` meanwhile told an operator they controlled the multiple-comparison method while its only reader echoes the string into a report body.
**Alternatives considered:** (a) Leave them and keep the comments — rejected: D-089 found, fixed and logged exactly this defect for `abstention.target_abstention_pct`; the same discipline applies. (b) Thread an `EffectiveConfig` into `judge_contribution` and `benjamini_hochberg` so a project can override them — deferred, not rejected: it is a signature change across the scorer and the kill switch, and is recorded as an open item.
**Rationale:** What this buys today is a single source of truth and an operator-visible, bounded field (alpha open in (0,1); confidence in (0.5,1)); what it does not yet buy is a per-project override, and saying so plainly is the point of this entry. The rubric is exposed read-only (`MappingProxyType`) because a governed threshold with a public setter is not one.
**Date 2026-07-26.**

## D-101 Hot-path purity becomes an allowlist, and `--root` starts working
**Decision:** `scripts/purity_check.py` fails on ANY third-party top-level import that is not in `ALLOWED_EXTERNAL` (`psycopg`, `psycopg_pool`, `pydantic`, `pydantic_settings`) or the standard library, rather than only on an 11-name denylist of known generative SDKs. `--root` is repeatable and actually selects what is walked; the default set is `tracebed.hotpath` PLUS `tracebed.workflow.prefetch`.
**Context:** Invariant 1's reachability walk was genuinely structural, but "no provider SDK" was a fixed list — `import groq` or `from ollama import chat` inside `hotpath/` passed the gate, proved by mutation. Separately `--root` was parsed and then ignored (the gate iterated a hardcoded `hotpath` glob), so retrieval-adjacent code outside `hotpath/` could not be checked by the gate that exists to check it.
**Alternatives considered:** (a) Extend the denylist — rejected: the set of things that are a generative client is open-ended and the set of things the hot path needs is closed and short. (b) Move `workflow/prefetch.py` into `hotpath/` — rejected: a bigger refactor than the gate needs, and the flag was supposed to work anyway.
**Rationale:** Adding a permitted package is now a reviewable one-line diff to an allowlist; adding a provider SDK without touching that line is impossible. Both halves are covered by `--self-test` (a `groq` import is rejected; `psycopg` and `hashlib` are not), so the gate proves its own bite.
**Date 2026-07-26.**

## D-102 Leak-suite probe 4 stops grepping for the word "dashboard"
**Decision:** Probe 4 extracts the `/v1/*` and `/admin/*` paths the dashboard's own TypeScript calls and probes those: every one must be registered on the app, every one must refuse an unauthenticated caller (401/405), and every one that answers 200 for project A must contain none of project B's identifiers.
**Context:** The old probe asserted that no registered route path contains the substring "dashboard". The dashboard shipped and talks to `/admin/*` and `/v1/*`, so the tripwire could never fire — and `gate_report_full.md` reported it PASS while asserting "no dashboard app exists in this tree yet".
**Alternatives considered:** (a) Delete the probe — rejected: PHASE-0 Task 17 names seven probe classes and a missing one is worse than a narrow one. (b) Hand-list the routes — rejected: a hand list drifts from the app the moment a view adds a call; extracting from source keeps the probe honest by construction, and the extraction itself is asserted non-empty.
**Rationale:** The anonymous-read half runs with no stack at all, which is what makes it a gate on this machine; per-route row-level isolation for the report routes still needs Postgres and is covered by probe 7's RLS backstop, which is stated in the module docstring rather than implied.
**Date 2026-07-26.**

## D-103 CI runs the whole offline suite, the offline harnesses, and every phase gate
**Decision:** `.github/workflows/ci.yml`'s static job runs `pytest -m "not integration"` (was `-m "phase0 and not integration"`) plus `guessed_reward`, `failopen_drill`, `consolidation_regression` and `workflow_scope` as scripts; the integration job runs phase 0–4 gates and `full_gate.py` and uploads all six reports. `tests/phase3/test_guessed_reward_drill.py` makes invariant 8's drill collectable by pytest.
**Context:** CI ran 1,587 of 3,721 collected tests. Never executed in CI: the negative probes, the render property tests, the red team, the guessed-reward drill, and gates 1–4 — four of the six harnesses the build prompt named as running on every commit, and the one the prompt named CI-blocking by rule 5. `harness/guessed_reward.py` has no `def test_*` and is not named `test_*.py`, so pytest never collected it and its only caller was a gate no job ran.
**Alternatives considered:** (a) Rename the harness to `test_*.py` — rejected: `harness/` is not a test root and `phase3_gate.py` imports it as a library; a thin pytest module that calls the same function keeps one implementation with two entry points. (b) Keep CI fast — rejected: the offline suite is about 35 seconds.
**Rationale:** A gate nothing runs is a script. The phase gates exit non-zero unless their verdict is PASS, and INCOMPLETE is explicitly not a pass, so with services present these are real gates rather than report generators.
**Date 2026-07-26.**

## D-104 Three undeclared dependencies are declared; D-036's `onnxruntime` line is corrected
**Decision:** `prometheus-client` (Apache-2.0, runtime), `pytest-asyncio` (Apache-2.0, dev) and `types-pyyaml` (Apache-2.0, dev) are recorded here with their licences, as hard rule 10 and D-036 require. D-036's inventory line naming `onnxruntime` as the secondary embedding driver is WRONG in this tree: `onnxruntime` is not in `pyproject.toml` and has never been installed. `scripts/license_check.py --dependency-audit` now fails if any declared dependency is absent from this file.
**Context:** D-036 promises "every future addition gets its own entry with license". Three dependencies arrived without one, and the same entry lists a package that does not exist here — so the dependency inventory was wrong in both directions at once.
**Alternatives considered:** (a) Add `onnxruntime` to make D-036 true — rejected: the `onnx-local` driver raises `ConfigError` unconditionally (see D-107), so the dependency would buy nothing but weight and a false capability claim. (b) Log without a check — rejected: this is the second time the inventory drifted; a check costs less than the next audit.
**Rationale:** Licences: `prometheus-client` Apache-2.0, `pytest-asyncio` Apache-2.0, `types-pyyaml` Apache-2.0 — all inside D-005's allowlist, none copyleft, none new legal surface. The audit flag is what makes the next omission a red build rather than a finding.
**Date 2026-07-26.**

## D-105 The RLS policy uses the `NULLIF(...)` form, not PLAN.md §5's literal text
**Decision:** Every policy in `migrations/0003_rls.sql` compares against `NULLIF(current_setting('tracebed.project_id', true), '')::uuid` rather than PLAN.md §5's bare `current_setting(...)::uuid`.
**Context:** `migrations/0003_rls.sql` says at its head that this deviation "needs a DECISIONS.md entry at merge; it is reported, not silent". No such entry was ever written. This is it, late.
**Alternatives considered:** PLAN.md's literal form — rejected: on a connection where the GUC was never set, `''::uuid` raises `invalid input syntax for type uuid` inside the policy, which turns a missing-GUC bug into a 500 on an unrelated statement instead of into "zero rows". The `NULLIF` form makes an unset GUC compare as NULL, so the policy denies everything, which is the fail-closed reading.
**Rationale:** Deny-on-unset is the behaviour invariant 4 wants; the plan's text was a sketch, not a decision. Logged rather than reverted, and the deviating text is quoted here so a reader of PLAN.md §5 finds the difference explained instead of discovering it in a migration.
**Date 2026-07-26.**

## D-106 `candidate → validated` requires the human verdict AND corroboration
**Decision:** `domain.state_machine`'s guard ANDs `has_verified_human_verdict` with `provenance_class is HUMAN_VERDICT`; PLAN.md §5 offers the human verdict as an ALTERNATIVE to corroboration (OR).
**Context:** Found by audit as an unlogged tightening. The shipped code is stricter than the plan.
**Alternatives considered:** Relax to the plan's OR — rejected: the two clauses answer different questions (who says so, and what class of evidence it is), and a human verdict recorded against a non-human-verdict provenance class is a bookkeeping error, not a promotion.
**Rationale:** A tightening is still a deviation. Logged rather than changed, because the strict reading cannot promote anything the plan's reading would refuse.
**Date 2026-07-26.**

## D-107 `embedding.secondary_driver` is named `embedding.driver`, and `onnx-local` is unimplemented
**Decision:** The config field PLAN.md §6 calls `embedding.secondary_driver` ships as `embedding.driver: Literal["gemini", "onnx-local"]`, and selecting `onnx-local` raises `ConfigError` at startup (`api/main.py`): it needs an injected tokenizer that no deployment in this repository names.
**Context:** D-007 calls the local ONNX driver "a fully supported secondary for air-gapped deployments" and D-008 sells it to customers who will not send query text off-box; PLAN.md calls the swap "one config line". Both are false today in the direction that matters, and neither the rename nor the unimplemented branch was logged.
**Alternatives considered:** (a) Rename the field back to `secondary_driver` — rejected: there is one driver slot and calling it "secondary" describes a list that does not exist. (b) Silently accept `onnx-local` and fall back to Gemini — rejected outright: that would send the query text off-box for exactly the deployment that chose the field to stop it. (c) Implement the driver — out of scope here; recorded in PLAN.md's known-gaps section with a size.
**Rationale:** Failing loudly at startup is the only honest behaviour for a privacy control that is not implemented. D-007's and D-008's claims are corrected by this entry rather than edited in place.
**Date 2026-07-26.**

## D-108 Gate "Known gaps" sections are pinned by a test; three false entries removed
**Decision:** `tests/phase4/test_known_gaps_are_current.py` checks each mechanically-checkable gap claim in BOTH directions — a declared gap must still be real, and a real gap must still be declared. Three entries that had become false were deleted: `abstention.target_abstention_pct` described as absent from `AbstentionConfig` (D-089 added it), and the blackboard column types plus the three `AgentControlRepoPort` methods described as missing (D-087/D-088 added them). `phase4_gate`'s dangling pointer to an "untracked-failures section" that renders only when failures exist was reworded.
**Context:** `_KNOWN_GAPS` is a hardcoded tuple in each gate script, derived from nothing and pinned by nothing, so re-running a gate reproduced false text verbatim. A reviewer trusts that section instead of reading the code.
**Alternatives considered:** Derive every gap from the run — rejected as unachievable for the judgement-shaped entries ("this needs a human decision at the STOP"); pretending those are mechanically checkable would be its own dishonesty. They are left as prose and excluded from the test by name.
**Rationale:** The second direction is the one that matters: fixing a defect now turns a test red until someone deletes the sentence claiming it is broken.
**Date 2026-07-26.**

## D-109 Container images are declared in a policy file and checked
**Decision:** `scripts/image_policy.toml` lists every container image `docker/compose.yaml` and `.github/workflows/ci.yml` may use, with its licence and an explicit floating-tag acknowledgement; `scripts/image_check.py` fails on an undeclared image and on an undeclared floating tag.
**Context:** `scripts/license_check.py` walks `importlib.metadata.distributions()` only, so container images were outside every gate — including `tensorchord/vchord-suite:pg18-latest`, an image named in no plan and no decision, on a floating tag. Hard rule 9's three named AGPL hazards (pg_search, MinIO, Redis) are all infrastructure, i.e. exactly the category the enforcement did not reach.
**Alternatives considered:** (a) Pin every image by digest — rejected here: resolving a digest needs network access this machine does not have, and a wrong digest breaks CI in a way that looks like a code failure. The policy records the floating tag as a KNOWN risk with an owner instead of hiding it. (b) Extend `license_check.py` — rejected: it audits Python distributions, and mixing two package universes into one script makes both harder to read.
**Rationale:** The check cannot verify a licence it is not told about, and that is the point: adding an image to compose without adding it to the policy fails the build, which is the smallest mechanism that would have caught the untracked one.
**Date 2026-07-26.**

## D-110 New DECISIONS entries must carry all five mandated fields
**Decision:** `harness/phase4_gate.py`'s DECISIONS check now validates that every entry from D-095 onward carries **Decision**, **Context**, **Alternatives considered**, **Rationale** and **Date**. Entries D-001 to D-094 are frozen as a legacy allowlist.
**Context:** 32 of the first 94 entries are malformed against the format this file's own header mandates, concentrated in D-009 to D-038, and three ids are out of file order — which removes the one structural signal a reader could use to sanity-check append-only in the absence of git history. The gate checked heading uniqueness and the presence of a date, and nothing else.
**Alternatives considered:** (a) Rewrite the 32 legacy entries — rejected: the file is append-only and rewriting history to satisfy a linter is precisely the edit the header forbids. (b) Fail on all 94 — rejected: a gate that is red on arrival gets suppressed.
**Rationale:** The rule binds going forward, where it can still be obeyed, and the legacy set is named rather than silently exempted.
**Date 2026-07-26.**

## D-111 The five phase STOPs did not happen, and the tree has no commits
**Decision:** Recorded, not remediated: no human approval gate ran between any two phases, and `git rev-list --all --count` is 0.
**Context:** The build prompt made human approval a precondition for each next phase and D-035 records that "the five phase STOPs remain non-negotiable". All six gate reports were generated inside 58 seconds (11:27:48 to 11:28:45 on 2026-07-26) AFTER Phase 4 was complete, and each still ends with retroactive STOP text. Separately, every path in the repository is untracked, which makes the prompt's test-first mandate and this file's own "never edit an entry" guarantee permanently unverifiable.
**Alternatives considered:** (a) Say nothing, since neither can be undone — rejected: an unrecorded process failure is indistinguishable from one that did not happen, and both facts change how much weight a reader should give every other entry in this file. (b) Regenerate the reports with honest timestamps — rejected: that would forge the record rather than correct it.
**Rationale:** Everything from roughly D-038 onward — the hot path, both learning lanes, promotion, the kill switch, all of Phase 4 — was decided without the review that was supposed to gate it. The remedy is a real STOP before the next phase (the worker plane) and an initial commit; both are listed in PLAN.md's known-gaps section as work, not as history.
**Date 2026-07-26.**

## D-112 The learning plane's unbuilt half is enumerated in PLAN.md rather than implied
**Decision:** PLAN.md gains a "Known gaps against the original spec" section listing, with rough sizes, everything the two plans promise that this tree does not do: no `UPDATE memory_item` anywhere (so no Q update, decay, strike, promotion, retirement, archive or tombstone can be saved), `workers/runner.py` constructing `handlers={}`, no Postgres implementation for ten worker ports, no writer for `memory_link`/`derived_state`/`killswitch_state`/`scoring_epoch`, no static-prefix delivery, no shadow-confirmation producer, no credit-assignment join, no episodic memory, no contradiction detection, no session-scoped working memory, no audit sink, no memory-edit write routes, no `memory.mode` surface, no TTL classes, no export-hook suggestion event, no policy-suggestion channel, no JIT wiring, no durable `routing_record`, and revalidation that is idle-triggered rather than usage-triggered.
**Context:** PLAN.md's own open-items list said "none of them silent" while omitting the four largest gaps in the tree, and its build-status appendix was stale. The in-code disclosure is genuinely good — 79 `CONTRACT GAP` comments — but no reader consults 79 comments; they read the summary.
**Alternatives considered:** (a) Build them here — rejected: the audit sizes the worker plane alone at 10 to 15 engineer-days, and building it without a STOP would repeat D-111's failure. (b) Split it into a numbered decision each — rejected: they are one absence with many faces, and a list a human can read in one screen is the point.
**Rationale:** A deployed Tracebed today ingests traces and outcome events faithfully and learns nothing from either. That sentence belongs in the plan, in the open, above the signature line — not distributed across forty modules.
**Date 2026-07-26.**

## D-113 Revalidation is idle-triggered; MEMORY_PLAN §10.4 specifies usage-triggered
**Decision:** `workers.revalidation` computes "due" from time-since-reference in a periodic batch, not from a retrieval.
**Context:** MEMORY_PLAN §10.4 specifies that "the retrieval itself is the trigger, so cost tracks usage, not vault size". Compounding the deviation: nothing writes `last_retrieved_at`, so the reference is always `created_at`, and a passing re-verification writes `last_revalidated_at`, which the due-test never reads — so every validated row re-verifies forever and cost tracks vault size, the exact property the spec's design existed to avoid.
**Alternatives considered:** Fix it here — rejected: the fix needs a `last_retrieved_at` write on the hot path (invariant 5 makes that asynchronous, so it needs the write plane) and the due-test needs to read `last_revalidated_at`. Both are inside the unbuilt worker plane (D-112) and are listed there with a size.
**Rationale:** Logged now because the shape of the deviation — a cost model inverted from the one the spec chose deliberately — is the kind that gets rationalised as "batch is simpler" once nobody remembers there was a decision.
**Date 2026-07-26.**

## D-114 The blackboard writes one status, not three
**Decision:** `stores.pg.blackboard` writes `committed` and nothing else; there is no `proposed` row, no `rejected` row and no branch-merge operation.
**Context:** MEMORY_PLAN §11 specifies propose, orchestrator validates, flip status in a transaction. D-088 covered the column types only, so the collapse of the merge protocol itself was never logged.
**Alternatives considered:** (a) Implement the three-status protocol — rejected here: it needs an orchestrator seam that does not exist and would be built blind. (b) Remove the `status` column — rejected: the column is the protocol's only surviving hook and dropping it would make the gap invisible.
**Rationale:** A single-status blackboard is a shared key-value store with a `status` column that always says the same thing. That is useful and it is not what §11 describes; the difference is now recorded rather than inferable only from a bound parameter.
**Date 2026-07-26.**

## D-115 Three archetype configs are DEPLOYMENT shapes, not MEMORY_PLAN §16's seven agent archetypes
**Decision:** The shipped archetype `.toml` files describe deployment profiles (general purpose, high volume, air-gapped-leaning), not the seven agent archetypes MEMORY_PLAN §16 enumerates.
**Context:** §16's archetypes are a taxonomy of AGENTS (what kind of work an agent does, which memory types it should be allowed); the shipped files are a taxonomy of INSTALLATIONS (throughput, locality). The substitution was never logged, and the two are not interchangeable: nothing in the tree lets an operator say "this agent type is a triage bot, so give it lessons and no preferences".
**Alternatives considered:** Ship seven agent archetypes now — rejected: without the `memory.mode` / per-type opt-in surface (also unbuilt, D-112) they would be seven files nothing reads.
**Rationale:** The deployment profiles are genuinely useful and the user asked for "good for all, no number-one focus", which they serve. The agent taxonomy is a separate, still-missing feature and is listed as such.
**Date 2026-07-26.**

## D-116 `POST /v1/propose_memory` has no agent-control mode check
**Decision:** The route enqueues a proposal for any authenticated principal; PLAN.md §3's API contract says "agent_control mode only".
**Context:** The `memory.mode` / per-type opt-in surface MEMORY_PLAN §16 specifies does not exist anywhere in the tree, so there is nothing for a mode check to read. Blast radius is bounded by D-023's per-run and per-project-day proposal caps and by the fact that a proposal enters at `candidate` under Tier B rules, but the access control the contract names is absent.
**Alternatives considered:** (a) Invent a boolean `agent_control.enabled` — rejected: it would be a third config surface that §16's real design would then have to supersede, and a check against a field this repository invented is not the check the contract names. (b) Disable the route until the surface exists — rejected: it is a Phase 4 deliverable with working caps and tests, and disabling it hides the gap rather than recording it.
**Rationale:** Logged as an open access-control gap with its mitigations named, and listed in PLAN.md's known-gaps section against the surface it depends on.
**Date 2026-07-26.**

## D-117 Gate-report honesty: three substitutions corrected, two disclosed, one claim withdrawn
**Decision:** (1) Phase 0 clause 3's "insert-without-verdict raises" half is now backed by two NAMED tests that exercise the real failure mode (a verdict minted for other content is refused with zero SQL issued; provenance is checked before the verdict), and the clause FAILS if either test stops being selected — it is no longer satisfied by the observation that `scan_verdict` is a required positional parameter. (2) Phase 3's dependence drill and lift sim, and Phase 2's sweep-cost drill, now print a `SIMULATED` note BESIDE the number rather than only in the report's known-gaps prose; the sweep-cost clause additionally states which half of it is a construction. (3) D-035(a)'s claim that "the latency bench reports at every gate" is WITHDRAWN: it reports at exactly one (`harness/phase1_gate.py` is the only importer), which is the correct design and was described wrongly.
**Context:** The audit's summary of the five gate reports was that they are "scrupulous about skips and careless about substitutions — they will not call an unrun test green, but they will call a different test the right test". Four clauses were satisfied by adjacent, weaker properties, one of which the runner itself called "adjacent"; three headline numbers restated their own simulator's inputs, with each caveat present in the same report's prose and absent beside the figure a reader actually quotes.
**Alternatives considered:** (a) Delete the simulated drills — rejected: they test real production functions (`compute_stratified_lift`, `assign_arm`, the sweep queries) and the simulation is the only way to exercise them without a stack. The defect was the labelling, not the drill. (b) Make the latency bench run at every gate to make D-035(a) true — rejected: it needs Postgres and 50 x 100k rows; running it five times would make four gates slower and no more honest.
**Rationale:** A gate must never claim PASS for something that did not execute, and must never measure something adjacent to its clause. The first rule was already honoured; this entry is about the second. Two substitutions remain and are NOT fixed here, so they are named instead: Phase 0 clause 1's "fake-runtime run -> complete queryable trace" is still proven by two unit suites rather than end to end, and Phase 2 clause 1's first half ("seeded failure traces -> expected Tier A notes") still has no affirmative assertion — only the zero-passthrough half is covered. Both need fixtures that do not exist yet and are listed in PLAN.md §11.
**Date 2026-07-26.**

## D-118 D-093's rejection of the three aggregate report routes is REVERSED — and the hazard it named is now real
**Decision:** `GET /admin/lift/report`, `GET /admin/staleness/report` and `GET /admin/consolidation/diffs` EXIST, at `src/tracebed/api/reports.py:181`, `:442` and `:522`, together with `GET /admin/injections`. D-093 rejected exactly these three and its rejection is superseded, not amended. This entry supersedes D-093's alternative (b) only; the seven read-only routes D-093 added, and its refusal of a kill-switch write route, both stand unchanged.
**Context:** D-093 was written by the dashboard integrator, which deleted three views (D-094) rather than add these routes. That integrator then crashed on a transient API error mid-rewrite of `App.tsx` and took the three view FILES with it. The crash and the deliberate deletion were indistinguishable on disk — three files named in a build report, absent from the tree, no commits to diff against — and the operator diagnosed it as data loss and ordered a rebuild. The rebuild needed routes, so a sibling agent built them. Neither agent read D-093, because nothing in the workflow required it to. The reversal was therefore not a decision anyone took; it was a decision that happened.
**Alternatives considered:** (a) Delete the routes and re-delete the views, restoring D-093 — rejected: the views are now built, audited, routed and reachable, and D-094's own rationale says the views "can return the moment something persists what they were drawing". Three of the four routes do exactly that (staleness and injections read persisted rows; consolidation reads the harness metric). (b) Leave both entries standing and record nothing — rejected outright: an audit trail whose last two entries describe a system that does not exist is worse than no trail, because it is trusted. The fidelity audit found precisely this and classified it DEVIATION-SILENT.
**Rationale:** D-093's stated reason for the rejection was correct and is now a live defect, so it is recorded here rather than quietly dropped: it warned that a route recomputing a governing number "would be a second author of a governing number whose first author is `workers/killswitch.py`". That is what shipped. `api/reports.py:159-178` `_bh_adjusted_p_values` is a SECOND, independent Benjamini-Hochberg implementation standing alongside `workers/killswitch.py:264` `benjamini_hochberg`. Two implementations of the same correction can disagree, and the one an operator reads in the dashboard is not the one that arms the kill switch. THIS IS NOT FIXED. The fix is for `reports.lift_report` to read what `workers.killswitch` computed rather than recompute it, which requires a table that persists killswitch evaluations — the same missing-writer class as `q_history` and the consolidation-diff table. Logged in PLAN.md §11 as an open item. Until then the dashboard's lift figures and the kill switch's own decision are computed by different code from the same inputs, and a reviewer should treat agreement between them as unverified.
**Date 2026-07-27.**

## D-119 D-094's deletion of three dashboard views is REVERSED
**Decision:** `dashboard/src/views/LiftAndQ.tsx`, `Staleness.tsx` and `Consolidation.tsx` exist, are routed (`dashboard/src/App.tsx:74-76`) and are in the nav (`dashboard/src/components/Layout.tsx:52-74`). D-094's decision to delete them is superseded. `Health.tsx` and `Projects.tsx` were rebuilt too and are live. D-094's deletion of every `FIXTURE_*` constant STANDS and is verified: a grep of `dashboard/src` for `FIXTURE_|fixtureData|MOCK_` returns nothing, and `dashboard/README.md` states "no view in this tree is fixture-backed".
**Context:** Same crash as D-118. The three files were rebuilt from scratch against `KillSwitch.tsx` as the reference, carrying forward all 22 defects the prior auditor had found in the originals, then audited again.
**Alternatives considered:** (a) Re-delete them to honour D-094 — rejected: D-094's rationale was "a removed view is honest; a view that renders a plausible number nobody measured is worse than no view", and that objection is met differently now. Each view is backed by a real route, and `Consolidation` renders an honest empty page with an on-page explanation because nothing writes consolidation sweeps yet. (b) Keep them and say nothing — rejected for the same reason as D-118(b).
**Rationale:** D-094's standard was that a view must not invent data. That standard is met: the fixtures are gone, the routes are real, and where a route has no writer the view says so on the page rather than filling the space. What D-094 got right and this entry preserves is the underlying gap — `q_history`, a consolidation-diff table and `memory_link` over HTTP still do not exist, so `MemoryDetail`'s Q history, `Consolidation`'s sweep list and `Forensics`' descendant chain are empty by construction rather than by absence of data. The views now disclose that; they do not fill it. A reader comparing this entry to D-094 should conclude that the views came back and the missing writers did not.
**Date 2026-07-27.**
